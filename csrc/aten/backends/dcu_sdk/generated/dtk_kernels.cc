// Copyright (c) 2026, BAAI. All rights reserved.
//
// @generated by scripts/codegen_dtk.py -- DO NOT EDIT.
//
// DCU SDK-native kernels. These link the DTK SDK and hipcc-compiled device
// code only -- never DTK's libtorch_hip.so/libc10_hip.so -- so the plugin is
// independent of the vendor's PyTorch build. That independence is the whole
// point of this backend; keep it when adding categories.

#include "../../../generated/ops.h"
#include "../dtk_launch.h"
#include <flagos.h>

#include <ATen/core/Tensor.h>
#include <ATen/ExpandUtils.h>
#include <ATen/ops/empty.h>
#include <ATen/ops/result_type.h>

namespace at::native::flagos::dcu_sdk {

namespace dtk_ops = at::native::flagos::dtk_ops;

// abs.out: elementwise unary into a caller-provided out. HIP kernel compiled by
// hipcc -- no vendor math library and no torch device symbols involved.
at::Tensor& AbsOutKernelDtk(const at::Tensor& self, at::Tensor& out) {
  // aten allows an undersized/empty out; the structured contract is to resize.
  if (!out.sizes().equals(self.sizes())) {
    out.resize_(self.sizes());
  }
  if (out.numel() == 0) return out;
  // Contiguous copy of the input: also breaks any aliasing with `out`, so an
  // in-place style call (out.is_same(self)) cannot read already-written data.
  auto in = self.contiguous();
  // Compute into `out` directly only when its layout lets us index it linearly.
  const bool direct = out.is_contiguous() && out.scalar_type() == self.scalar_type();
  auto dst = direct ? out : at::empty(self.sizes(), out.options().dtype(self.scalar_type()));
  dtk_ops::LaunchUnary(
      in.data_ptr(), dst.data_ptr(), dst.numel(), self.scalar_type(),
      dtk_ops::UnaryOp::ABS,
      ::GetCurrentStreamForDevice(self.device().index()));
  if (!direct) out.copy_(dst);
  return out;
}

// acos.out: elementwise unary into a caller-provided out. HIP kernel compiled by
// hipcc -- no vendor math library and no torch device symbols involved.
at::Tensor& AcosOutKernelDtk(const at::Tensor& self, at::Tensor& out) {
  // aten allows an undersized/empty out; the structured contract is to resize.
  if (!out.sizes().equals(self.sizes())) {
    out.resize_(self.sizes());
  }
  if (out.numel() == 0) return out;
  // Contiguous copy of the input: also breaks any aliasing with `out`, so an
  // in-place style call (out.is_same(self)) cannot read already-written data.
  auto in = self.contiguous();
  // Compute into `out` directly only when its layout lets us index it linearly.
  const bool direct = out.is_contiguous() && out.scalar_type() == self.scalar_type();
  auto dst = direct ? out : at::empty(self.sizes(), out.options().dtype(self.scalar_type()));
  dtk_ops::LaunchUnary(
      in.data_ptr(), dst.data_ptr(), dst.numel(), self.scalar_type(),
      dtk_ops::UnaryOp::ACOS,
      ::GetCurrentStreamForDevice(self.device().index()));
  if (!direct) out.copy_(dst);
  return out;
}

// acosh.out: elementwise unary into a caller-provided out. HIP kernel compiled by
// hipcc -- no vendor math library and no torch device symbols involved.
at::Tensor& AcoshOutKernelDtk(const at::Tensor& self, at::Tensor& out) {
  // aten allows an undersized/empty out; the structured contract is to resize.
  if (!out.sizes().equals(self.sizes())) {
    out.resize_(self.sizes());
  }
  if (out.numel() == 0) return out;
  // Contiguous copy of the input: also breaks any aliasing with `out`, so an
  // in-place style call (out.is_same(self)) cannot read already-written data.
  auto in = self.contiguous();
  // Compute into `out` directly only when its layout lets us index it linearly.
  const bool direct = out.is_contiguous() && out.scalar_type() == self.scalar_type();
  auto dst = direct ? out : at::empty(self.sizes(), out.options().dtype(self.scalar_type()));
  dtk_ops::LaunchUnary(
      in.data_ptr(), dst.data_ptr(), dst.numel(), self.scalar_type(),
      dtk_ops::UnaryOp::ACOSH,
      ::GetCurrentStreamForDevice(self.device().index()));
  if (!direct) out.copy_(dst);
  return out;
}

// asin.out: elementwise unary into a caller-provided out. HIP kernel compiled by
// hipcc -- no vendor math library and no torch device symbols involved.
at::Tensor& AsinOutKernelDtk(const at::Tensor& self, at::Tensor& out) {
  // aten allows an undersized/empty out; the structured contract is to resize.
  if (!out.sizes().equals(self.sizes())) {
    out.resize_(self.sizes());
  }
  if (out.numel() == 0) return out;
  // Contiguous copy of the input: also breaks any aliasing with `out`, so an
  // in-place style call (out.is_same(self)) cannot read already-written data.
  auto in = self.contiguous();
  // Compute into `out` directly only when its layout lets us index it linearly.
  const bool direct = out.is_contiguous() && out.scalar_type() == self.scalar_type();
  auto dst = direct ? out : at::empty(self.sizes(), out.options().dtype(self.scalar_type()));
  dtk_ops::LaunchUnary(
      in.data_ptr(), dst.data_ptr(), dst.numel(), self.scalar_type(),
      dtk_ops::UnaryOp::ASIN,
      ::GetCurrentStreamForDevice(self.device().index()));
  if (!direct) out.copy_(dst);
  return out;
}

// atan.out: elementwise unary into a caller-provided out. HIP kernel compiled by
// hipcc -- no vendor math library and no torch device symbols involved.
at::Tensor& AtanOutKernelDtk(const at::Tensor& self, at::Tensor& out) {
  // aten allows an undersized/empty out; the structured contract is to resize.
  if (!out.sizes().equals(self.sizes())) {
    out.resize_(self.sizes());
  }
  if (out.numel() == 0) return out;
  // Contiguous copy of the input: also breaks any aliasing with `out`, so an
  // in-place style call (out.is_same(self)) cannot read already-written data.
  auto in = self.contiguous();
  // Compute into `out` directly only when its layout lets us index it linearly.
  const bool direct = out.is_contiguous() && out.scalar_type() == self.scalar_type();
  auto dst = direct ? out : at::empty(self.sizes(), out.options().dtype(self.scalar_type()));
  dtk_ops::LaunchUnary(
      in.data_ptr(), dst.data_ptr(), dst.numel(), self.scalar_type(),
      dtk_ops::UnaryOp::ATAN,
      ::GetCurrentStreamForDevice(self.device().index()));
  if (!direct) out.copy_(dst);
  return out;
}

// atanh.out: elementwise unary into a caller-provided out. HIP kernel compiled by
// hipcc -- no vendor math library and no torch device symbols involved.
at::Tensor& AtanhOutKernelDtk(const at::Tensor& self, at::Tensor& out) {
  // aten allows an undersized/empty out; the structured contract is to resize.
  if (!out.sizes().equals(self.sizes())) {
    out.resize_(self.sizes());
  }
  if (out.numel() == 0) return out;
  // Contiguous copy of the input: also breaks any aliasing with `out`, so an
  // in-place style call (out.is_same(self)) cannot read already-written data.
  auto in = self.contiguous();
  // Compute into `out` directly only when its layout lets us index it linearly.
  const bool direct = out.is_contiguous() && out.scalar_type() == self.scalar_type();
  auto dst = direct ? out : at::empty(self.sizes(), out.options().dtype(self.scalar_type()));
  dtk_ops::LaunchUnary(
      in.data_ptr(), dst.data_ptr(), dst.numel(), self.scalar_type(),
      dtk_ops::UnaryOp::ATANH,
      ::GetCurrentStreamForDevice(self.device().index()));
  if (!direct) out.copy_(dst);
  return out;
}

// bitwise_and.Tensor_out: elementwise binary into a caller-provided out. Broadcasting and dtype
// promotion are resolved with aten's utilities; only the inner loop is ours.
at::Tensor& BitwiseAndTensorOutKernelDtk(
    const at::Tensor& self, const at::Tensor& other, at::Tensor& out) {
  auto promoted = at::result_type(self, other);
  auto shape = at::infer_size(self.sizes(), other.sizes());
  if (!out.sizes().equals(shape)) {
    out.resize_(shape);
  }
  if (out.numel() == 0) return out;
  // expand().contiguous() materializes the broadcast operands and, as with the
  // unary case, breaks aliasing between the inputs and `out`.
  auto a = self.to(promoted).expand(shape).contiguous();
  auto b = other.to(promoted).expand(shape).contiguous();
  const bool direct = out.is_contiguous() && out.scalar_type() == promoted;
  auto dst = direct ? out : at::empty(shape, out.options().dtype(promoted));
  dtk_ops::LaunchBinary(
      a.data_ptr(), b.data_ptr(), dst.data_ptr(), dst.numel(), promoted,
      dtk_ops::BinaryOp::BITWISE_AND_TENSOR,
      ::GetCurrentStreamForDevice(self.device().index()));
  if (!direct) out.copy_(dst);
  return out;
}

// bitwise_or.Tensor_out: elementwise binary into a caller-provided out. Broadcasting and dtype
// promotion are resolved with aten's utilities; only the inner loop is ours.
at::Tensor& BitwiseOrTensorOutKernelDtk(
    const at::Tensor& self, const at::Tensor& other, at::Tensor& out) {
  auto promoted = at::result_type(self, other);
  auto shape = at::infer_size(self.sizes(), other.sizes());
  if (!out.sizes().equals(shape)) {
    out.resize_(shape);
  }
  if (out.numel() == 0) return out;
  // expand().contiguous() materializes the broadcast operands and, as with the
  // unary case, breaks aliasing between the inputs and `out`.
  auto a = self.to(promoted).expand(shape).contiguous();
  auto b = other.to(promoted).expand(shape).contiguous();
  const bool direct = out.is_contiguous() && out.scalar_type() == promoted;
  auto dst = direct ? out : at::empty(shape, out.options().dtype(promoted));
  dtk_ops::LaunchBinary(
      a.data_ptr(), b.data_ptr(), dst.data_ptr(), dst.numel(), promoted,
      dtk_ops::BinaryOp::BITWISE_OR_TENSOR,
      ::GetCurrentStreamForDevice(self.device().index()));
  if (!direct) out.copy_(dst);
  return out;
}

// bitwise_xor.Tensor_out: elementwise binary into a caller-provided out. Broadcasting and dtype
// promotion are resolved with aten's utilities; only the inner loop is ours.
at::Tensor& BitwiseXorTensorOutKernelDtk(
    const at::Tensor& self, const at::Tensor& other, at::Tensor& out) {
  auto promoted = at::result_type(self, other);
  auto shape = at::infer_size(self.sizes(), other.sizes());
  if (!out.sizes().equals(shape)) {
    out.resize_(shape);
  }
  if (out.numel() == 0) return out;
  // expand().contiguous() materializes the broadcast operands and, as with the
  // unary case, breaks aliasing between the inputs and `out`.
  auto a = self.to(promoted).expand(shape).contiguous();
  auto b = other.to(promoted).expand(shape).contiguous();
  const bool direct = out.is_contiguous() && out.scalar_type() == promoted;
  auto dst = direct ? out : at::empty(shape, out.options().dtype(promoted));
  dtk_ops::LaunchBinary(
      a.data_ptr(), b.data_ptr(), dst.data_ptr(), dst.numel(), promoted,
      dtk_ops::BinaryOp::BITWISE_XOR_TENSOR,
      ::GetCurrentStreamForDevice(self.device().index()));
  if (!direct) out.copy_(dst);
  return out;
}

// cos.out: elementwise unary into a caller-provided out. HIP kernel compiled by
// hipcc -- no vendor math library and no torch device symbols involved.
at::Tensor& CosOutKernelDtk(const at::Tensor& self, at::Tensor& out) {
  // aten allows an undersized/empty out; the structured contract is to resize.
  if (!out.sizes().equals(self.sizes())) {
    out.resize_(self.sizes());
  }
  if (out.numel() == 0) return out;
  // Contiguous copy of the input: also breaks any aliasing with `out`, so an
  // in-place style call (out.is_same(self)) cannot read already-written data.
  auto in = self.contiguous();
  // Compute into `out` directly only when its layout lets us index it linearly.
  const bool direct = out.is_contiguous() && out.scalar_type() == self.scalar_type();
  auto dst = direct ? out : at::empty(self.sizes(), out.options().dtype(self.scalar_type()));
  dtk_ops::LaunchUnary(
      in.data_ptr(), dst.data_ptr(), dst.numel(), self.scalar_type(),
      dtk_ops::UnaryOp::COS,
      ::GetCurrentStreamForDevice(self.device().index()));
  if (!direct) out.copy_(dst);
  return out;
}

// erf.out: elementwise unary into a caller-provided out. HIP kernel compiled by
// hipcc -- no vendor math library and no torch device symbols involved.
at::Tensor& ErfOutKernelDtk(const at::Tensor& self, at::Tensor& out) {
  // aten allows an undersized/empty out; the structured contract is to resize.
  if (!out.sizes().equals(self.sizes())) {
    out.resize_(self.sizes());
  }
  if (out.numel() == 0) return out;
  // Contiguous copy of the input: also breaks any aliasing with `out`, so an
  // in-place style call (out.is_same(self)) cannot read already-written data.
  auto in = self.contiguous();
  // Compute into `out` directly only when its layout lets us index it linearly.
  const bool direct = out.is_contiguous() && out.scalar_type() == self.scalar_type();
  auto dst = direct ? out : at::empty(self.sizes(), out.options().dtype(self.scalar_type()));
  dtk_ops::LaunchUnary(
      in.data_ptr(), dst.data_ptr(), dst.numel(), self.scalar_type(),
      dtk_ops::UnaryOp::ERF,
      ::GetCurrentStreamForDevice(self.device().index()));
  if (!direct) out.copy_(dst);
  return out;
}

// erfc.out: elementwise unary into a caller-provided out. HIP kernel compiled by
// hipcc -- no vendor math library and no torch device symbols involved.
at::Tensor& ErfcOutKernelDtk(const at::Tensor& self, at::Tensor& out) {
  // aten allows an undersized/empty out; the structured contract is to resize.
  if (!out.sizes().equals(self.sizes())) {
    out.resize_(self.sizes());
  }
  if (out.numel() == 0) return out;
  // Contiguous copy of the input: also breaks any aliasing with `out`, so an
  // in-place style call (out.is_same(self)) cannot read already-written data.
  auto in = self.contiguous();
  // Compute into `out` directly only when its layout lets us index it linearly.
  const bool direct = out.is_contiguous() && out.scalar_type() == self.scalar_type();
  auto dst = direct ? out : at::empty(self.sizes(), out.options().dtype(self.scalar_type()));
  dtk_ops::LaunchUnary(
      in.data_ptr(), dst.data_ptr(), dst.numel(), self.scalar_type(),
      dtk_ops::UnaryOp::ERFC,
      ::GetCurrentStreamForDevice(self.device().index()));
  if (!direct) out.copy_(dst);
  return out;
}

// exp2.out: elementwise unary into a caller-provided out. HIP kernel compiled by
// hipcc -- no vendor math library and no torch device symbols involved.
at::Tensor& Exp2OutKernelDtk(const at::Tensor& self, at::Tensor& out) {
  // aten allows an undersized/empty out; the structured contract is to resize.
  if (!out.sizes().equals(self.sizes())) {
    out.resize_(self.sizes());
  }
  if (out.numel() == 0) return out;
  // Contiguous copy of the input: also breaks any aliasing with `out`, so an
  // in-place style call (out.is_same(self)) cannot read already-written data.
  auto in = self.contiguous();
  // Compute into `out` directly only when its layout lets us index it linearly.
  const bool direct = out.is_contiguous() && out.scalar_type() == self.scalar_type();
  auto dst = direct ? out : at::empty(self.sizes(), out.options().dtype(self.scalar_type()));
  dtk_ops::LaunchUnary(
      in.data_ptr(), dst.data_ptr(), dst.numel(), self.scalar_type(),
      dtk_ops::UnaryOp::EXP2,
      ::GetCurrentStreamForDevice(self.device().index()));
  if (!direct) out.copy_(dst);
  return out;
}

// frac.out: elementwise unary into a caller-provided out. HIP kernel compiled by
// hipcc -- no vendor math library and no torch device symbols involved.
at::Tensor& FracOutKernelDtk(const at::Tensor& self, at::Tensor& out) {
  // aten allows an undersized/empty out; the structured contract is to resize.
  if (!out.sizes().equals(self.sizes())) {
    out.resize_(self.sizes());
  }
  if (out.numel() == 0) return out;
  // Contiguous copy of the input: also breaks any aliasing with `out`, so an
  // in-place style call (out.is_same(self)) cannot read already-written data.
  auto in = self.contiguous();
  // Compute into `out` directly only when its layout lets us index it linearly.
  const bool direct = out.is_contiguous() && out.scalar_type() == self.scalar_type();
  auto dst = direct ? out : at::empty(self.sizes(), out.options().dtype(self.scalar_type()));
  dtk_ops::LaunchUnary(
      in.data_ptr(), dst.data_ptr(), dst.numel(), self.scalar_type(),
      dtk_ops::UnaryOp::FRAC,
      ::GetCurrentStreamForDevice(self.device().index()));
  if (!direct) out.copy_(dst);
  return out;
}

// gcd.out: elementwise binary into a caller-provided out. Broadcasting and dtype
// promotion are resolved with aten's utilities; only the inner loop is ours.
at::Tensor& GcdOutKernelDtk(
    const at::Tensor& self, const at::Tensor& other, at::Tensor& out) {
  auto promoted = at::result_type(self, other);
  auto shape = at::infer_size(self.sizes(), other.sizes());
  if (!out.sizes().equals(shape)) {
    out.resize_(shape);
  }
  if (out.numel() == 0) return out;
  // expand().contiguous() materializes the broadcast operands and, as with the
  // unary case, breaks aliasing between the inputs and `out`.
  auto a = self.to(promoted).expand(shape).contiguous();
  auto b = other.to(promoted).expand(shape).contiguous();
  const bool direct = out.is_contiguous() && out.scalar_type() == promoted;
  auto dst = direct ? out : at::empty(shape, out.options().dtype(promoted));
  dtk_ops::LaunchBinary(
      a.data_ptr(), b.data_ptr(), dst.data_ptr(), dst.numel(), promoted,
      dtk_ops::BinaryOp::GCD,
      ::GetCurrentStreamForDevice(self.device().index()));
  if (!direct) out.copy_(dst);
  return out;
}

// hypot.out: elementwise binary into a caller-provided out. Broadcasting and dtype
// promotion are resolved with aten's utilities; only the inner loop is ours.
at::Tensor& HypotOutKernelDtk(
    const at::Tensor& self, const at::Tensor& other, at::Tensor& out) {
  auto promoted = at::result_type(self, other);
  auto shape = at::infer_size(self.sizes(), other.sizes());
  if (!out.sizes().equals(shape)) {
    out.resize_(shape);
  }
  if (out.numel() == 0) return out;
  // expand().contiguous() materializes the broadcast operands and, as with the
  // unary case, breaks aliasing between the inputs and `out`.
  auto a = self.to(promoted).expand(shape).contiguous();
  auto b = other.to(promoted).expand(shape).contiguous();
  const bool direct = out.is_contiguous() && out.scalar_type() == promoted;
  auto dst = direct ? out : at::empty(shape, out.options().dtype(promoted));
  dtk_ops::LaunchBinary(
      a.data_ptr(), b.data_ptr(), dst.data_ptr(), dst.numel(), promoted,
      dtk_ops::BinaryOp::HYPOT,
      ::GetCurrentStreamForDevice(self.device().index()));
  if (!direct) out.copy_(dst);
  return out;
}

// log.out: elementwise unary into a caller-provided out. HIP kernel compiled by
// hipcc -- no vendor math library and no torch device symbols involved.
at::Tensor& LogOutKernelDtk(const at::Tensor& self, at::Tensor& out) {
  // aten allows an undersized/empty out; the structured contract is to resize.
  if (!out.sizes().equals(self.sizes())) {
    out.resize_(self.sizes());
  }
  if (out.numel() == 0) return out;
  // Contiguous copy of the input: also breaks any aliasing with `out`, so an
  // in-place style call (out.is_same(self)) cannot read already-written data.
  auto in = self.contiguous();
  // Compute into `out` directly only when its layout lets us index it linearly.
  const bool direct = out.is_contiguous() && out.scalar_type() == self.scalar_type();
  auto dst = direct ? out : at::empty(self.sizes(), out.options().dtype(self.scalar_type()));
  dtk_ops::LaunchUnary(
      in.data_ptr(), dst.data_ptr(), dst.numel(), self.scalar_type(),
      dtk_ops::UnaryOp::LOG,
      ::GetCurrentStreamForDevice(self.device().index()));
  if (!direct) out.copy_(dst);
  return out;
}

// log2.out: elementwise unary into a caller-provided out. HIP kernel compiled by
// hipcc -- no vendor math library and no torch device symbols involved.
at::Tensor& Log2OutKernelDtk(const at::Tensor& self, at::Tensor& out) {
  // aten allows an undersized/empty out; the structured contract is to resize.
  if (!out.sizes().equals(self.sizes())) {
    out.resize_(self.sizes());
  }
  if (out.numel() == 0) return out;
  // Contiguous copy of the input: also breaks any aliasing with `out`, so an
  // in-place style call (out.is_same(self)) cannot read already-written data.
  auto in = self.contiguous();
  // Compute into `out` directly only when its layout lets us index it linearly.
  const bool direct = out.is_contiguous() && out.scalar_type() == self.scalar_type();
  auto dst = direct ? out : at::empty(self.sizes(), out.options().dtype(self.scalar_type()));
  dtk_ops::LaunchUnary(
      in.data_ptr(), dst.data_ptr(), dst.numel(), self.scalar_type(),
      dtk_ops::UnaryOp::LOG2,
      ::GetCurrentStreamForDevice(self.device().index()));
  if (!direct) out.copy_(dst);
  return out;
}

// nextafter.out: elementwise binary into a caller-provided out. Broadcasting and dtype
// promotion are resolved with aten's utilities; only the inner loop is ours.
at::Tensor& NextafterOutKernelDtk(
    const at::Tensor& self, const at::Tensor& other, at::Tensor& out) {
  auto promoted = at::result_type(self, other);
  auto shape = at::infer_size(self.sizes(), other.sizes());
  if (!out.sizes().equals(shape)) {
    out.resize_(shape);
  }
  if (out.numel() == 0) return out;
  // expand().contiguous() materializes the broadcast operands and, as with the
  // unary case, breaks aliasing between the inputs and `out`.
  auto a = self.to(promoted).expand(shape).contiguous();
  auto b = other.to(promoted).expand(shape).contiguous();
  const bool direct = out.is_contiguous() && out.scalar_type() == promoted;
  auto dst = direct ? out : at::empty(shape, out.options().dtype(promoted));
  dtk_ops::LaunchBinary(
      a.data_ptr(), b.data_ptr(), dst.data_ptr(), dst.numel(), promoted,
      dtk_ops::BinaryOp::NEXTAFTER,
      ::GetCurrentStreamForDevice(self.device().index()));
  if (!direct) out.copy_(dst);
  return out;
}

} // namespace at::native::flagos::dcu_sdk
