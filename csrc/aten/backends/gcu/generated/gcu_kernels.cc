// Copyright (c) 2026, BAAI. All rights reserved.
//
// @generated by scripts/codegen/codegen_gcu.py -- DO NOT EDIT.
//
// topsaten kernels for the Enflame GCU backend, generated per-category. Each
// kernel wraps its aten tensors into topsatenTensors and issues one direct
// topsaten call via EXEC_TOPSATEN_CMD. Dispatchers are declared in
// generated/ops.h (shared with the CUDA codegen); here we only fill the
// Backend::kGcu slot.

#include "../../../generated/ops.h"
#include <ATen/core/Tensor.h>
#include <ATen/ExpandUtils.h>
#include <ATen/native/RangeUtils.h>
#include <ATen/ops/all.h>
#include <ATen/ops/arange.h>
#include <ATen/ops/empty.h>
#include <ATen/ops/_amp_foreach_non_finite_check_and_unscale.h>
#include <ATen/ops/convolution.h>
#include <ATen/ops/convolution_backward.h>
#include <ATen/ops/embedding_dense_backward.h>
#include <ATen/ops/index.h>
#include <ATen/ops/index_select.h>
#include <ATen/ops/linalg_vector_norm.h>
#include <ATen/ops/nonzero.h>
#include <ATen/ops/nonzero_static.h>
#include <ATen/ops/result_type.h>
#include <ATen/ops/where.h>
#include <ATen/ops/zeros.h>
#include <ATen/ops/_upsample_nearest_exact2d.h>
#include <ATen/native/ConvUtils.h>
#include <ATen/core/List.h>
#include <c10/core/Scalar.h>
#include <algorithm>
#include <limits>
#include <optional>
#include <string>
#include <vector>
#include "../topsaten_common.h"
#include "runtime/functions.h"

namespace at::native::flagos {

namespace gcu = at::native::flagos::gcu;

at::Tensor AbsKernelGcu(const at::Tensor& self) {
  if (!gcu::TopsatenSupportsDtype(self.scalar_type())) {
    return at::abs(self.cpu()).to(self.device());
  }
  auto out = at::empty(self.sizes(), self.options());
  gcu::TopsatenTensorWrapper t_self(self);
  gcu::TopsatenTensorWrapper t_out(out);
  EXEC_TOPSATEN_CMD(topsatenAbs, self, t_out.get(), t_self.get());
  return out;
}

REGISTER_IMPL_TO_DISPATCHER(AbsFn, abs_dispatcher, Backend::kGcu, AbsKernelGcu)

at::Tensor SqrtKernelGcu(const at::Tensor& self) {
  if (!gcu::TopsatenSupportsDtype(self.scalar_type())) {
    return at::sqrt(self.cpu()).to(self.device());
  }
  auto out = at::empty(self.sizes(), self.options());
  gcu::TopsatenTensorWrapper t_self(self);
  gcu::TopsatenTensorWrapper t_out(out);
  EXEC_TOPSATEN_CMD(topsatenSqrt, self, t_out.get(), t_self.get());
  return out;
}

REGISTER_IMPL_TO_DISPATCHER(SqrtFn, sqrt_dispatcher, Backend::kGcu, SqrtKernelGcu)

at::Tensor RsqrtKernelGcu(const at::Tensor& self) {
  if (!gcu::TopsatenSupportsDtype(self.scalar_type())) {
    return at::rsqrt(self.cpu()).to(self.device());
  }
  auto out = at::empty(self.sizes(), self.options());
  gcu::TopsatenTensorWrapper t_self(self);
  gcu::TopsatenTensorWrapper t_out(out);
  EXEC_TOPSATEN_CMD(topsatenRsqrt, self, t_out.get(), t_self.get());
  return out;
}

REGISTER_IMPL_TO_DISPATCHER(RsqrtFn, rsqrt_dispatcher, Backend::kGcu, RsqrtKernelGcu)

at::Tensor ExpKernelGcu(const at::Tensor& self) {
  if (!gcu::TopsatenSupportsDtype(self.scalar_type())) {
    return at::exp(self.cpu()).to(self.device());
  }
  auto out = at::empty(self.sizes(), self.options());
  gcu::TopsatenTensorWrapper t_self(self);
  gcu::TopsatenTensorWrapper t_out(out);
  EXEC_TOPSATEN_CMD(topsatenExp, self, t_out.get(), t_self.get());
  return out;
}

REGISTER_IMPL_TO_DISPATCHER(ExpFn, exp_dispatcher, Backend::kGcu, ExpKernelGcu)

at::Tensor Expm1KernelGcu(const at::Tensor& self) {
  if (!gcu::TopsatenSupportsDtype(self.scalar_type())) {
    return at::expm1(self.cpu()).to(self.device());
  }
  auto out = at::empty(self.sizes(), self.options());
  gcu::TopsatenTensorWrapper t_self(self);
  gcu::TopsatenTensorWrapper t_out(out);
  EXEC_TOPSATEN_CMD(topsatenExpm1, self, t_out.get(), t_self.get());
  return out;
}

REGISTER_IMPL_TO_DISPATCHER(Expm1Fn, expm1_dispatcher, Backend::kGcu, Expm1KernelGcu)

at::Tensor LogKernelGcu(const at::Tensor& self) {
  if (!gcu::TopsatenSupportsDtype(self.scalar_type())) {
    return at::log(self.cpu()).to(self.device());
  }
  auto out = at::empty(self.sizes(), self.options());
  gcu::TopsatenTensorWrapper t_self(self);
  gcu::TopsatenTensorWrapper t_out(out);
  EXEC_TOPSATEN_CMD(topsatenLog, self, t_out.get(), t_self.get());
  return out;
}

REGISTER_IMPL_TO_DISPATCHER(LogFn, log_dispatcher, Backend::kGcu, LogKernelGcu)

at::Tensor Log2KernelGcu(const at::Tensor& self) {
  if (!gcu::TopsatenSupportsDtype(self.scalar_type())) {
    return at::log2(self.cpu()).to(self.device());
  }
  auto out = at::empty(self.sizes(), self.options());
  gcu::TopsatenTensorWrapper t_self(self);
  gcu::TopsatenTensorWrapper t_out(out);
  EXEC_TOPSATEN_CMD(topsatenLog2, self, t_out.get(), t_self.get());
  return out;
}

REGISTER_IMPL_TO_DISPATCHER(Log2Fn, log2_dispatcher, Backend::kGcu, Log2KernelGcu)

at::Tensor Log10KernelGcu(const at::Tensor& self) {
  if (!gcu::TopsatenSupportsDtype(self.scalar_type())) {
    return at::log10(self.cpu()).to(self.device());
  }
  auto out = at::empty(self.sizes(), self.options());
  gcu::TopsatenTensorWrapper t_self(self);
  gcu::TopsatenTensorWrapper t_out(out);
  EXEC_TOPSATEN_CMD(topsatenLog10, self, t_out.get(), t_self.get());
  return out;
}

REGISTER_IMPL_TO_DISPATCHER(Log10Fn, log10_dispatcher, Backend::kGcu, Log10KernelGcu)

at::Tensor Log1pKernelGcu(const at::Tensor& self) {
  if (!gcu::TopsatenSupportsDtype(self.scalar_type())) {
    return at::log1p(self.cpu()).to(self.device());
  }
  auto out = at::empty(self.sizes(), self.options());
  gcu::TopsatenTensorWrapper t_self(self);
  gcu::TopsatenTensorWrapper t_out(out);
  EXEC_TOPSATEN_CMD(topsatenLog1p, self, t_out.get(), t_self.get());
  return out;
}

REGISTER_IMPL_TO_DISPATCHER(Log1pFn, log1p_dispatcher, Backend::kGcu, Log1pKernelGcu)

at::Tensor SinKernelGcu(const at::Tensor& self) {
  if (!gcu::TopsatenSupportsDtype(self.scalar_type())) {
    return at::sin(self.cpu()).to(self.device());
  }
  auto out = at::empty(self.sizes(), self.options());
  gcu::TopsatenTensorWrapper t_self(self);
  gcu::TopsatenTensorWrapper t_out(out);
  EXEC_TOPSATEN_CMD(topsatenSin, self, t_out.get(), t_self.get());
  return out;
}

REGISTER_IMPL_TO_DISPATCHER(SinFn, sin_dispatcher, Backend::kGcu, SinKernelGcu)

at::Tensor CosKernelGcu(const at::Tensor& self) {
  if (!gcu::TopsatenSupportsDtype(self.scalar_type())) {
    return at::cos(self.cpu()).to(self.device());
  }
  auto out = at::empty(self.sizes(), self.options());
  gcu::TopsatenTensorWrapper t_self(self);
  gcu::TopsatenTensorWrapper t_out(out);
  EXEC_TOPSATEN_CMD(topsatenCos, self, t_out.get(), t_self.get());
  return out;
}

REGISTER_IMPL_TO_DISPATCHER(CosFn, cos_dispatcher, Backend::kGcu, CosKernelGcu)

at::Tensor SinhKernelGcu(const at::Tensor& self) {
  if (!gcu::TopsatenSupportsDtype(self.scalar_type())) {
    return at::sinh(self.cpu()).to(self.device());
  }
  auto out = at::empty(self.sizes(), self.options());
  gcu::TopsatenTensorWrapper t_self(self);
  gcu::TopsatenTensorWrapper t_out(out);
  EXEC_TOPSATEN_CMD(topsatenSinh, self, t_out.get(), t_self.get());
  return out;
}

REGISTER_IMPL_TO_DISPATCHER(SinhFn, sinh_dispatcher, Backend::kGcu, SinhKernelGcu)

at::Tensor CoshKernelGcu(const at::Tensor& self) {
  if (!gcu::TopsatenSupportsDtype(self.scalar_type())) {
    return at::cosh(self.cpu()).to(self.device());
  }
  auto out = at::empty(self.sizes(), self.options());
  gcu::TopsatenTensorWrapper t_self(self);
  gcu::TopsatenTensorWrapper t_out(out);
  EXEC_TOPSATEN_CMD(topsatenCosh, self, t_out.get(), t_self.get());
  return out;
}

REGISTER_IMPL_TO_DISPATCHER(CoshFn, cosh_dispatcher, Backend::kGcu, CoshKernelGcu)

at::Tensor AsinKernelGcu(const at::Tensor& self) {
  if (!gcu::TopsatenSupportsDtype(self.scalar_type())) {
    return at::asin(self.cpu()).to(self.device());
  }
  auto out = at::empty(self.sizes(), self.options());
  gcu::TopsatenTensorWrapper t_self(self);
  gcu::TopsatenTensorWrapper t_out(out);
  EXEC_TOPSATEN_CMD(topsatenAsin, self, t_out.get(), t_self.get());
  return out;
}

REGISTER_IMPL_TO_DISPATCHER(AsinFn, asin_dispatcher, Backend::kGcu, AsinKernelGcu)

at::Tensor AcosKernelGcu(const at::Tensor& self) {
  if (!gcu::TopsatenSupportsDtype(self.scalar_type())) {
    return at::acos(self.cpu()).to(self.device());
  }
  auto out = at::empty(self.sizes(), self.options());
  gcu::TopsatenTensorWrapper t_self(self);
  gcu::TopsatenTensorWrapper t_out(out);
  EXEC_TOPSATEN_CMD(topsatenAcos, self, t_out.get(), t_self.get());
  return out;
}

REGISTER_IMPL_TO_DISPATCHER(AcosFn, acos_dispatcher, Backend::kGcu, AcosKernelGcu)

at::Tensor AtanKernelGcu(const at::Tensor& self) {
  if (!gcu::TopsatenSupportsDtype(self.scalar_type())) {
    return at::atan(self.cpu()).to(self.device());
  }
  auto out = at::empty(self.sizes(), self.options());
  gcu::TopsatenTensorWrapper t_self(self);
  gcu::TopsatenTensorWrapper t_out(out);
  EXEC_TOPSATEN_CMD(topsatenAtan, self, t_out.get(), t_self.get());
  return out;
}

REGISTER_IMPL_TO_DISPATCHER(AtanFn, atan_dispatcher, Backend::kGcu, AtanKernelGcu)

at::Tensor TanhKernelGcu(const at::Tensor& self) {
  if (!gcu::TopsatenSupportsDtype(self.scalar_type())) {
    return at::tanh(self.cpu()).to(self.device());
  }
  auto out = at::empty(self.sizes(), self.options());
  gcu::TopsatenTensorWrapper t_self(self);
  gcu::TopsatenTensorWrapper t_out(out);
  EXEC_TOPSATEN_CMD(topsatenTanh, self, t_out.get(), t_self.get());
  return out;
}

REGISTER_IMPL_TO_DISPATCHER(TanhFn, tanh_dispatcher, Backend::kGcu, TanhKernelGcu)

at::Tensor SigmoidKernelGcu(const at::Tensor& self) {
  if (!gcu::TopsatenSupportsDtype(self.scalar_type())) {
    return at::sigmoid(self.cpu()).to(self.device());
  }
  auto out = at::empty(self.sizes(), self.options());
  gcu::TopsatenTensorWrapper t_self(self);
  gcu::TopsatenTensorWrapper t_out(out);
  EXEC_TOPSATEN_CMD(topsatenSigmoid, self, t_out.get(), t_self.get());
  return out;
}

REGISTER_IMPL_TO_DISPATCHER(SigmoidFn, sigmoid_dispatcher, Backend::kGcu, SigmoidKernelGcu)

at::Tensor SiluKernelGcu(const at::Tensor& self) {
  if (!gcu::TopsatenSupportsDtype(self.scalar_type())) {
    return at::silu(self.cpu()).to(self.device());
  }
  auto out = at::empty(self.sizes(), self.options());
  gcu::TopsatenTensorWrapper t_self(self);
  gcu::TopsatenTensorWrapper t_out(out);
  EXEC_TOPSATEN_CMD(topsatenSilu, self, t_out.get(), t_self.get());
  return out;
}

REGISTER_IMPL_TO_DISPATCHER(SiluFn, silu_dispatcher, Backend::kGcu, SiluKernelGcu)

at::Tensor ReluKernelGcu(const at::Tensor& self) {
  if (!gcu::TopsatenSupportsDtype(self.scalar_type())) {
    return at::relu(self.cpu()).to(self.device());
  }
  auto out = at::empty(self.sizes(), self.options());
  gcu::TopsatenTensorWrapper t_self(self);
  gcu::TopsatenTensorWrapper t_out(out);
  EXEC_TOPSATEN_CMD(topsatenRelu, self, t_out.get(), t_self.get());
  return out;
}

REGISTER_IMPL_TO_DISPATCHER(ReluFn, relu_dispatcher, Backend::kGcu, ReluKernelGcu)

at::Tensor NegKernelGcu(const at::Tensor& self) {
  if (!gcu::TopsatenSupportsDtype(self.scalar_type())) {
    return at::neg(self.cpu()).to(self.device());
  }
  auto out = at::empty(self.sizes(), self.options());
  gcu::TopsatenTensorWrapper t_self(self);
  gcu::TopsatenTensorWrapper t_out(out);
  EXEC_TOPSATEN_CMD(topsatenNeg, self, t_out.get(), t_self.get());
  return out;
}

REGISTER_IMPL_TO_DISPATCHER(NegFn, neg_dispatcher, Backend::kGcu, NegKernelGcu)

at::Tensor ReciprocalKernelGcu(const at::Tensor& self) {
  if (!gcu::TopsatenSupportsDtype(self.scalar_type())) {
    return at::reciprocal(self.cpu()).to(self.device());
  }
  auto out = at::empty(self.sizes(), self.options());
  gcu::TopsatenTensorWrapper t_self(self);
  gcu::TopsatenTensorWrapper t_out(out);
  EXEC_TOPSATEN_CMD(topsatenReciprocal, self, t_out.get(), t_self.get());
  return out;
}

REGISTER_IMPL_TO_DISPATCHER(ReciprocalFn, reciprocal_dispatcher, Backend::kGcu, ReciprocalKernelGcu)

at::Tensor ErfKernelGcu(const at::Tensor& self) {
  if (!gcu::TopsatenSupportsDtype(self.scalar_type())) {
    return at::erf(self.cpu()).to(self.device());
  }
  auto out = at::empty(self.sizes(), self.options());
  gcu::TopsatenTensorWrapper t_self(self);
  gcu::TopsatenTensorWrapper t_out(out);
  EXEC_TOPSATEN_CMD(topsatenErf, self, t_out.get(), t_self.get());
  return out;
}

REGISTER_IMPL_TO_DISPATCHER(ErfFn, erf_dispatcher, Backend::kGcu, ErfKernelGcu)

at::Tensor FloorKernelGcu(const at::Tensor& self) {
  if (!gcu::TopsatenSupportsDtype(self.scalar_type())) {
    return at::floor(self.cpu()).to(self.device());
  }
  auto out = at::empty(self.sizes(), self.options());
  gcu::TopsatenTensorWrapper t_self(self);
  gcu::TopsatenTensorWrapper t_out(out);
  EXEC_TOPSATEN_CMD(topsatenFloor, self, t_out.get(), t_self.get());
  return out;
}

REGISTER_IMPL_TO_DISPATCHER(FloorFn, floor_dispatcher, Backend::kGcu, FloorKernelGcu)

at::Tensor CeilKernelGcu(const at::Tensor& self) {
  if (!gcu::TopsatenSupportsDtype(self.scalar_type())) {
    return at::ceil(self.cpu()).to(self.device());
  }
  auto out = at::empty(self.sizes(), self.options());
  gcu::TopsatenTensorWrapper t_self(self);
  gcu::TopsatenTensorWrapper t_out(out);
  EXEC_TOPSATEN_CMD(topsatenCeil, self, t_out.get(), t_self.get());
  return out;
}

REGISTER_IMPL_TO_DISPATCHER(CeilFn, ceil_dispatcher, Backend::kGcu, CeilKernelGcu)

at::Tensor TruncKernelGcu(const at::Tensor& self) {
  if (!gcu::TopsatenSupportsDtype(self.scalar_type())) {
    return at::trunc(self.cpu()).to(self.device());
  }
  auto out = at::empty(self.sizes(), self.options());
  gcu::TopsatenTensorWrapper t_self(self);
  gcu::TopsatenTensorWrapper t_out(out);
  EXEC_TOPSATEN_CMD(topsatenTrunc, self, t_out.get(), t_self.get());
  return out;
}

REGISTER_IMPL_TO_DISPATCHER(TruncFn, trunc_dispatcher, Backend::kGcu, TruncKernelGcu)

at::Tensor SignKernelGcu(const at::Tensor& self) {
  if (!gcu::TopsatenSupportsDtype(self.scalar_type())) {
    return at::sign(self.cpu()).to(self.device());
  }
  auto out = at::empty(self.sizes(), self.options());
  gcu::TopsatenTensorWrapper t_self(self);
  gcu::TopsatenTensorWrapper t_out(out);
  EXEC_TOPSATEN_CMD(topsatenSign, self, t_out.get(), t_self.get());
  return out;
}

REGISTER_IMPL_TO_DISPATCHER(SignFn, sign_dispatcher, Backend::kGcu, SignKernelGcu)

at::Tensor MulTensorKernelGcu(const at::Tensor& self, const at::Tensor& other) {
  if (!gcu::TopsatenSupportsDtype(self.scalar_type()) ||
      !gcu::TopsatenSupportsDtype(other.scalar_type())) {
    return at::mul(self.cpu(), other.cpu()).to(self.device());
  }
  auto result_dtype = at::result_type(self, other);
  auto self_c = self.scalar_type() == result_dtype ? self : self.to(result_dtype);
  auto other_c = other.to(self.device(), result_dtype);
  auto out_shape = at::infer_size(self_c.sizes(), other_c.sizes());
  auto self_b = self_c.expand(out_shape).contiguous();
  auto other_b = other_c.expand(out_shape).contiguous();
  auto out = at::empty(out_shape, self.options().dtype(result_dtype));

  gcu::TopsatenTensorWrapper t_self(self_b);
  gcu::TopsatenTensorWrapper t_other(other_b);
  gcu::TopsatenTensorWrapper t_out(out);
  EXEC_TOPSATEN_CMD(topsatenMul, self, t_out.get(), t_self.get(), t_other.get());
  return out;
}

REGISTER_IMPL_TO_DISPATCHER(MulTensorFn, mul_tensor_dispatcher, Backend::kGcu, MulTensorKernelGcu)

at::Tensor DivTensorKernelGcu(const at::Tensor& self, const at::Tensor& other) {
  if (!gcu::TopsatenSupportsDtype(self.scalar_type()) ||
      !gcu::TopsatenSupportsDtype(other.scalar_type())) {
    return at::div(self.cpu(), other.cpu()).to(self.device());
  }
  auto result_dtype = at::result_type(self, other);
  auto self_c = self.scalar_type() == result_dtype ? self : self.to(result_dtype);
  auto other_c = other.to(self.device(), result_dtype);
  auto out_shape = at::infer_size(self_c.sizes(), other_c.sizes());
  auto self_b = self_c.expand(out_shape).contiguous();
  auto other_b = other_c.expand(out_shape).contiguous();
  auto out = at::empty(out_shape, self.options().dtype(result_dtype));

  gcu::TopsatenTensorWrapper t_self(self_b);
  gcu::TopsatenTensorWrapper t_other(other_b);
  gcu::TopsatenTensorWrapper t_out(out);
  EXEC_TOPSATEN_CMD(topsatenDiv, self, t_out.get(), t_self.get(), t_other.get());
  return out;
}

REGISTER_IMPL_TO_DISPATCHER(DivTensorFn, div_tensor_dispatcher, Backend::kGcu, DivTensorKernelGcu)

at::Tensor MaximumKernelGcu(const at::Tensor& self, const at::Tensor& other) {
  if (!gcu::TopsatenSupportsDtype(self.scalar_type()) ||
      !gcu::TopsatenSupportsDtype(other.scalar_type())) {
    return at::maximum(self.cpu(), other.cpu()).to(self.device());
  }
  auto result_dtype = at::result_type(self, other);
  auto self_c = self.scalar_type() == result_dtype ? self : self.to(result_dtype);
  auto other_c = other.to(self.device(), result_dtype);
  auto out_shape = at::infer_size(self_c.sizes(), other_c.sizes());
  auto self_b = self_c.expand(out_shape).contiguous();
  auto other_b = other_c.expand(out_shape).contiguous();
  auto out = at::empty(out_shape, self.options().dtype(result_dtype));

  gcu::TopsatenTensorWrapper t_self(self_b);
  gcu::TopsatenTensorWrapper t_other(other_b);
  gcu::TopsatenTensorWrapper t_out(out);
  EXEC_TOPSATEN_CMD(topsatenMaximum, self, t_out.get(), t_self.get(), t_other.get());
  return out;
}

REGISTER_IMPL_TO_DISPATCHER(MaximumFn, maximum_dispatcher, Backend::kGcu, MaximumKernelGcu)

at::Tensor MinimumKernelGcu(const at::Tensor& self, const at::Tensor& other) {
  if (!gcu::TopsatenSupportsDtype(self.scalar_type()) ||
      !gcu::TopsatenSupportsDtype(other.scalar_type())) {
    return at::minimum(self.cpu(), other.cpu()).to(self.device());
  }
  auto result_dtype = at::result_type(self, other);
  auto self_c = self.scalar_type() == result_dtype ? self : self.to(result_dtype);
  auto other_c = other.to(self.device(), result_dtype);
  auto out_shape = at::infer_size(self_c.sizes(), other_c.sizes());
  auto self_b = self_c.expand(out_shape).contiguous();
  auto other_b = other_c.expand(out_shape).contiguous();
  auto out = at::empty(out_shape, self.options().dtype(result_dtype));

  gcu::TopsatenTensorWrapper t_self(self_b);
  gcu::TopsatenTensorWrapper t_other(other_b);
  gcu::TopsatenTensorWrapper t_out(out);
  EXEC_TOPSATEN_CMD(topsatenMinimum, self, t_out.get(), t_self.get(), t_other.get());
  return out;
}

REGISTER_IMPL_TO_DISPATCHER(MinimumFn, minimum_dispatcher, Backend::kGcu, MinimumKernelGcu)

at::Tensor RemainderTensorKernelGcu(const at::Tensor& self, const at::Tensor& other) {
  if (!gcu::TopsatenSupportsDtype(self.scalar_type()) ||
      !gcu::TopsatenSupportsDtype(other.scalar_type())) {
    return at::remainder(self.cpu(), other.cpu()).to(self.device());
  }
  auto result_dtype = at::result_type(self, other);
  auto self_c = self.scalar_type() == result_dtype ? self : self.to(result_dtype);
  auto other_c = other.to(self.device(), result_dtype);
  auto out_shape = at::infer_size(self_c.sizes(), other_c.sizes());
  auto self_b = self_c.expand(out_shape).contiguous();
  auto other_b = other_c.expand(out_shape).contiguous();
  auto out = at::empty(out_shape, self.options().dtype(result_dtype));

  gcu::TopsatenTensorWrapper t_self(self_b);
  gcu::TopsatenTensorWrapper t_other(other_b);
  gcu::TopsatenTensorWrapper t_out(out);
  EXEC_TOPSATEN_CMD(topsatenRemainder, self, t_out.get(), t_self.get(), t_other.get());
  return out;
}

REGISTER_IMPL_TO_DISPATCHER(RemainderTensorFn, remainder_tensor_dispatcher, Backend::kGcu, RemainderTensorKernelGcu)

at::Tensor FmodTensorKernelGcu(const at::Tensor& self, const at::Tensor& other) {
  if (!gcu::TopsatenSupportsDtype(self.scalar_type()) ||
      !gcu::TopsatenSupportsDtype(other.scalar_type())) {
    return at::fmod(self.cpu(), other.cpu()).to(self.device());
  }
  auto result_dtype = at::result_type(self, other);
  auto self_c = self.scalar_type() == result_dtype ? self : self.to(result_dtype);
  auto other_c = other.to(self.device(), result_dtype);
  auto out_shape = at::infer_size(self_c.sizes(), other_c.sizes());
  auto self_b = self_c.expand(out_shape).contiguous();
  auto other_b = other_c.expand(out_shape).contiguous();
  auto out = at::empty(out_shape, self.options().dtype(result_dtype));

  gcu::TopsatenTensorWrapper t_self(self_b);
  gcu::TopsatenTensorWrapper t_other(other_b);
  gcu::TopsatenTensorWrapper t_out(out);
  EXEC_TOPSATEN_CMD(topsatenFmod, self, t_out.get(), t_self.get(), t_other.get());
  return out;
}

REGISTER_IMPL_TO_DISPATCHER(FmodTensorFn, fmod_tensor_dispatcher, Backend::kGcu, FmodTensorKernelGcu)

at::Tensor PowTensorTensorKernelGcu(const at::Tensor& self, const at::Tensor& other) {
  if (!gcu::TopsatenSupportsDtype(self.scalar_type()) ||
      !gcu::TopsatenSupportsDtype(other.scalar_type())) {
    return at::pow(self.cpu(), other.cpu()).to(self.device());
  }
  auto result_dtype = at::result_type(self, other);
  auto self_c = self.scalar_type() == result_dtype ? self : self.to(result_dtype);
  auto other_c = other.to(self.device(), result_dtype);
  auto out_shape = at::infer_size(self_c.sizes(), other_c.sizes());
  auto self_b = self_c.expand(out_shape).contiguous();
  auto other_b = other_c.expand(out_shape).contiguous();
  auto out = at::empty(out_shape, self.options().dtype(result_dtype));

  gcu::TopsatenTensorWrapper t_self(self_b);
  gcu::TopsatenTensorWrapper t_other(other_b);
  gcu::TopsatenTensorWrapper t_out(out);
  EXEC_TOPSATEN_CMD(topsatenPow, self, t_out.get(), t_self.get(), t_other.get());
  return out;
}

REGISTER_IMPL_TO_DISPATCHER(PowTensorTensorFn, pow_tensor_tensor_dispatcher, Backend::kGcu, PowTensorTensorKernelGcu)

at::Tensor AddTensorKernelGcu(const at::Tensor& self, const at::Tensor& other, const at::Scalar& alpha) {
  if (!gcu::TopsatenSupportsDtype(self.scalar_type()) ||
      !gcu::TopsatenSupportsDtype(other.scalar_type())) {
    return at::add(self.cpu(), other.cpu(), alpha).to(self.device());
  }
  auto result_dtype = at::result_type(self, other);
  auto self_c = self.scalar_type() == result_dtype ? self : self.to(result_dtype);
  auto other_c = other.to(self.device(), result_dtype);
  auto out_shape = at::infer_size(self_c.sizes(), other_c.sizes());
  auto self_b = self_c.expand(out_shape).contiguous();
  auto other_b = other_c.expand(out_shape).contiguous();
  auto out = at::empty(out_shape, self.options().dtype(result_dtype));
  auto t_alpha = gcu::ToTopsatenScalar(alpha, result_dtype);

  gcu::TopsatenTensorWrapper t_self(self_b);
  gcu::TopsatenTensorWrapper t_other(other_b);
  gcu::TopsatenTensorWrapper t_out(out);
  EXEC_TOPSATEN_CMD(topsatenAdd, self, t_out.get(), t_self.get(), t_other.get(), t_alpha);
  return out;
}

REGISTER_IMPL_TO_DISPATCHER(AddTensorFn, add_tensor_dispatcher, Backend::kGcu, AddTensorKernelGcu)

at::Tensor SubTensorKernelGcu(const at::Tensor& self, const at::Tensor& other, const at::Scalar& alpha) {
  if (!gcu::TopsatenSupportsDtype(self.scalar_type()) ||
      !gcu::TopsatenSupportsDtype(other.scalar_type())) {
    return at::sub(self.cpu(), other.cpu(), alpha).to(self.device());
  }
  auto result_dtype = at::result_type(self, other);
  auto self_c = self.scalar_type() == result_dtype ? self : self.to(result_dtype);
  auto other_c = other.to(self.device(), result_dtype);
  auto out_shape = at::infer_size(self_c.sizes(), other_c.sizes());
  auto self_b = self_c.expand(out_shape).contiguous();
  auto other_b = other_c.expand(out_shape).contiguous();
  auto out = at::empty(out_shape, self.options().dtype(result_dtype));
  auto t_alpha = gcu::ToTopsatenScalar(alpha, result_dtype);

  gcu::TopsatenTensorWrapper t_self(self_b);
  gcu::TopsatenTensorWrapper t_other(other_b);
  gcu::TopsatenTensorWrapper t_out(out);
  EXEC_TOPSATEN_CMD(topsatenSub, self, t_out.get(), t_self.get(), t_other.get(), t_alpha);
  return out;
}

REGISTER_IMPL_TO_DISPATCHER(SubTensorFn, sub_tensor_dispatcher, Backend::kGcu, SubTensorKernelGcu)

at::Tensor& MulOutKernelGcu(const at::Tensor& self, const at::Tensor& other, at::Tensor& out) {
  gcu::TopsDeviceGuard out_guard(self);
  TORCH_CHECK(
      c10::canCast(at::result_type(self, other), out.scalar_type()),
      "result type ",
      at::result_type(self, other),
      " can't be cast to the desired output type ",
      out.scalar_type());
  if (!gcu::TopsatenSupportsDtype(self.scalar_type()) ||
      !gcu::TopsatenSupportsDtype(other.scalar_type())) {
    auto host = at::mul(self.cpu(), other.cpu());
    if (!out.sizes().equals(host.sizes())) {
      out.resize_(host.sizes());
    }
    out.copy_(host);
    return out;
  }
  auto result_dtype = at::result_type(self, other);
  auto self_c = self.scalar_type() == result_dtype ? self : self.to(result_dtype);
  auto other_c = other.to(self.device(), result_dtype);
  auto out_shape = at::infer_size(self_c.sizes(), other_c.sizes());
  auto self_b = self_c.expand(out_shape).contiguous();
  auto other_b = other_c.expand(out_shape).contiguous();
  if (!out.sizes().equals(out_shape)) {
    out.resize_(out_shape);
  }
  if (out.numel() == 0) {
    return out;
  }
  if (out.scalar_type() != result_dtype || !out.is_contiguous() ||
      out.device() != self.device()) {
    out.copy_(at::mul(self.cpu(), other.cpu()));
    return out;
  }

  gcu::TopsatenTensorWrapper t_self(self_b);
  gcu::TopsatenTensorWrapper t_other(other_b);
  gcu::TopsatenTensorWrapper t_out(out);
  EXEC_TOPSATEN_CMD(topsatenMul, out, t_out.get(), t_self.get(), t_other.get());
  return out;
}

REGISTER_IMPL_TO_DISPATCHER(MulOutFn, mul_out_dispatcher, Backend::kGcu, MulOutKernelGcu)

at::Tensor& AddOutKernelGcu(const at::Tensor& self, const at::Tensor& other, const at::Scalar& alpha, at::Tensor& out) {
  gcu::TopsDeviceGuard out_guard(self);
  TORCH_CHECK(
      c10::canCast(at::result_type(self, other), out.scalar_type()),
      "result type ",
      at::result_type(self, other),
      " can't be cast to the desired output type ",
      out.scalar_type());
  if (!gcu::TopsatenSupportsDtype(self.scalar_type()) ||
      !gcu::TopsatenSupportsDtype(other.scalar_type())) {
    auto host = at::add(self.cpu(), other.cpu(), alpha);
    if (!out.sizes().equals(host.sizes())) {
      out.resize_(host.sizes());
    }
    out.copy_(host);
    return out;
  }
  auto result_dtype = at::result_type(self, other);
  auto self_c = self.scalar_type() == result_dtype ? self : self.to(result_dtype);
  auto other_c = other.to(self.device(), result_dtype);
  auto out_shape = at::infer_size(self_c.sizes(), other_c.sizes());
  auto self_b = self_c.expand(out_shape).contiguous();
  auto other_b = other_c.expand(out_shape).contiguous();
  if (!out.sizes().equals(out_shape)) {
    out.resize_(out_shape);
  }
  if (out.numel() == 0) {
    return out;
  }
  if (out.scalar_type() != result_dtype || !out.is_contiguous() ||
      out.device() != self.device()) {
    out.copy_(at::add(self.cpu(), other.cpu(), alpha));
    return out;
  }
  auto t_alpha = gcu::ToTopsatenScalar(alpha, result_dtype);

  gcu::TopsatenTensorWrapper t_self(self_b);
  gcu::TopsatenTensorWrapper t_other(other_b);
  gcu::TopsatenTensorWrapper t_out(out);
  EXEC_TOPSATEN_CMD(topsatenAdd, out, t_out.get(), t_self.get(), t_other.get(), t_alpha);
  return out;
}

REGISTER_IMPL_TO_DISPATCHER(AddOutFn, add_out_dispatcher, Backend::kGcu, AddOutKernelGcu)

at::Tensor& SubOutKernelGcu(const at::Tensor& self, const at::Tensor& other, const at::Scalar& alpha, at::Tensor& out) {
  gcu::TopsDeviceGuard out_guard(self);
  TORCH_CHECK(
      c10::canCast(at::result_type(self, other), out.scalar_type()),
      "result type ",
      at::result_type(self, other),
      " can't be cast to the desired output type ",
      out.scalar_type());
  if (!gcu::TopsatenSupportsDtype(self.scalar_type()) ||
      !gcu::TopsatenSupportsDtype(other.scalar_type())) {
    auto host = at::sub(self.cpu(), other.cpu(), alpha);
    if (!out.sizes().equals(host.sizes())) {
      out.resize_(host.sizes());
    }
    out.copy_(host);
    return out;
  }
  auto result_dtype = at::result_type(self, other);
  auto self_c = self.scalar_type() == result_dtype ? self : self.to(result_dtype);
  auto other_c = other.to(self.device(), result_dtype);
  auto out_shape = at::infer_size(self_c.sizes(), other_c.sizes());
  auto self_b = self_c.expand(out_shape).contiguous();
  auto other_b = other_c.expand(out_shape).contiguous();
  if (!out.sizes().equals(out_shape)) {
    out.resize_(out_shape);
  }
  if (out.numel() == 0) {
    return out;
  }
  if (out.scalar_type() != result_dtype || !out.is_contiguous() ||
      out.device() != self.device()) {
    out.copy_(at::sub(self.cpu(), other.cpu(), alpha));
    return out;
  }
  auto t_alpha = gcu::ToTopsatenScalar(alpha, result_dtype);

  gcu::TopsatenTensorWrapper t_self(self_b);
  gcu::TopsatenTensorWrapper t_other(other_b);
  gcu::TopsatenTensorWrapper t_out(out);
  EXEC_TOPSATEN_CMD(topsatenSub, out, t_out.get(), t_self.get(), t_other.get(), t_alpha);
  return out;
}

REGISTER_IMPL_TO_DISPATCHER(SubOutFn, sub_out_dispatcher, Backend::kGcu, SubOutKernelGcu)

at::Tensor EqTensorKernelGcu(const at::Tensor& self, const at::Tensor& other) {
  if (!gcu::TopsatenSupportsDtype(self.scalar_type()) ||
      !gcu::TopsatenSupportsDtype(other.scalar_type())) {
    return at::eq(self.cpu(), other.cpu()).to(self.device());
  }
  auto result_dtype = at::result_type(self, other);
  auto self_c = self.scalar_type() == result_dtype ? self : self.to(result_dtype);
  auto other_c = other.to(self.device(), result_dtype);
  auto out_shape = at::infer_size(self_c.sizes(), other_c.sizes());
  auto self_b = self_c.expand(out_shape).contiguous();
  auto other_b = other_c.expand(out_shape).contiguous();
  auto out = at::empty(out_shape, self.options().dtype(at::kBool));

  gcu::TopsatenTensorWrapper t_self(self_b);
  gcu::TopsatenTensorWrapper t_other(other_b);
  gcu::TopsatenTensorWrapper t_out(out);
  EXEC_TOPSATEN_CMD(topsatenEq, self, t_out.get(), t_self.get(), t_other.get());
  return out;
}

REGISTER_IMPL_TO_DISPATCHER(EqTensorFn, eq_tensor_dispatcher, Backend::kGcu, EqTensorKernelGcu)

at::Tensor NeTensorKernelGcu(const at::Tensor& self, const at::Tensor& other) {
  if (!gcu::TopsatenSupportsDtype(self.scalar_type()) ||
      !gcu::TopsatenSupportsDtype(other.scalar_type())) {
    return at::ne(self.cpu(), other.cpu()).to(self.device());
  }
  auto result_dtype = at::result_type(self, other);
  auto self_c = self.scalar_type() == result_dtype ? self : self.to(result_dtype);
  auto other_c = other.to(self.device(), result_dtype);
  auto out_shape = at::infer_size(self_c.sizes(), other_c.sizes());
  auto self_b = self_c.expand(out_shape).contiguous();
  auto other_b = other_c.expand(out_shape).contiguous();
  auto out = at::empty(out_shape, self.options().dtype(at::kBool));

  gcu::TopsatenTensorWrapper t_self(self_b);
  gcu::TopsatenTensorWrapper t_other(other_b);
  gcu::TopsatenTensorWrapper t_out(out);
  EXEC_TOPSATEN_CMD(topsatenNe, self, t_out.get(), t_self.get(), t_other.get());
  return out;
}

REGISTER_IMPL_TO_DISPATCHER(NeTensorFn, ne_tensor_dispatcher, Backend::kGcu, NeTensorKernelGcu)

at::Tensor LtTensorKernelGcu(const at::Tensor& self, const at::Tensor& other) {
  if (!gcu::TopsatenSupportsDtype(self.scalar_type()) ||
      !gcu::TopsatenSupportsDtype(other.scalar_type())) {
    return at::lt(self.cpu(), other.cpu()).to(self.device());
  }
  auto result_dtype = at::result_type(self, other);
  auto self_c = self.scalar_type() == result_dtype ? self : self.to(result_dtype);
  auto other_c = other.to(self.device(), result_dtype);
  auto out_shape = at::infer_size(self_c.sizes(), other_c.sizes());
  auto self_b = self_c.expand(out_shape).contiguous();
  auto other_b = other_c.expand(out_shape).contiguous();
  auto out = at::empty(out_shape, self.options().dtype(at::kBool));

  gcu::TopsatenTensorWrapper t_self(self_b);
  gcu::TopsatenTensorWrapper t_other(other_b);
  gcu::TopsatenTensorWrapper t_out(out);
  EXEC_TOPSATEN_CMD(topsatenLt, self, t_out.get(), t_self.get(), t_other.get());
  return out;
}

REGISTER_IMPL_TO_DISPATCHER(LtTensorFn, lt_tensor_dispatcher, Backend::kGcu, LtTensorKernelGcu)

at::Tensor GtTensorKernelGcu(const at::Tensor& self, const at::Tensor& other) {
  if (!gcu::TopsatenSupportsDtype(self.scalar_type()) ||
      !gcu::TopsatenSupportsDtype(other.scalar_type())) {
    return at::gt(self.cpu(), other.cpu()).to(self.device());
  }
  auto result_dtype = at::result_type(self, other);
  auto self_c = self.scalar_type() == result_dtype ? self : self.to(result_dtype);
  auto other_c = other.to(self.device(), result_dtype);
  auto out_shape = at::infer_size(self_c.sizes(), other_c.sizes());
  auto self_b = self_c.expand(out_shape).contiguous();
  auto other_b = other_c.expand(out_shape).contiguous();
  auto out = at::empty(out_shape, self.options().dtype(at::kBool));

  gcu::TopsatenTensorWrapper t_self(self_b);
  gcu::TopsatenTensorWrapper t_other(other_b);
  gcu::TopsatenTensorWrapper t_out(out);
  EXEC_TOPSATEN_CMD(topsatenGt, self, t_out.get(), t_self.get(), t_other.get());
  return out;
}

REGISTER_IMPL_TO_DISPATCHER(GtTensorFn, gt_tensor_dispatcher, Backend::kGcu, GtTensorKernelGcu)

at::Tensor LeTensorKernelGcu(const at::Tensor& self, const at::Tensor& other) {
  if (!gcu::TopsatenSupportsDtype(self.scalar_type()) ||
      !gcu::TopsatenSupportsDtype(other.scalar_type())) {
    return at::le(self.cpu(), other.cpu()).to(self.device());
  }
  auto result_dtype = at::result_type(self, other);
  auto self_c = self.scalar_type() == result_dtype ? self : self.to(result_dtype);
  auto other_c = other.to(self.device(), result_dtype);
  auto out_shape = at::infer_size(self_c.sizes(), other_c.sizes());
  auto self_b = self_c.expand(out_shape).contiguous();
  auto other_b = other_c.expand(out_shape).contiguous();
  auto out = at::empty(out_shape, self.options().dtype(at::kBool));

  gcu::TopsatenTensorWrapper t_self(self_b);
  gcu::TopsatenTensorWrapper t_other(other_b);
  gcu::TopsatenTensorWrapper t_out(out);
  EXEC_TOPSATEN_CMD(topsatenLe, self, t_out.get(), t_self.get(), t_other.get());
  return out;
}

REGISTER_IMPL_TO_DISPATCHER(LeTensorFn, le_tensor_dispatcher, Backend::kGcu, LeTensorKernelGcu)

at::Tensor GeTensorKernelGcu(const at::Tensor& self, const at::Tensor& other) {
  if (!gcu::TopsatenSupportsDtype(self.scalar_type()) ||
      !gcu::TopsatenSupportsDtype(other.scalar_type())) {
    return at::ge(self.cpu(), other.cpu()).to(self.device());
  }
  auto result_dtype = at::result_type(self, other);
  auto self_c = self.scalar_type() == result_dtype ? self : self.to(result_dtype);
  auto other_c = other.to(self.device(), result_dtype);
  auto out_shape = at::infer_size(self_c.sizes(), other_c.sizes());
  auto self_b = self_c.expand(out_shape).contiguous();
  auto other_b = other_c.expand(out_shape).contiguous();
  auto out = at::empty(out_shape, self.options().dtype(at::kBool));

  gcu::TopsatenTensorWrapper t_self(self_b);
  gcu::TopsatenTensorWrapper t_other(other_b);
  gcu::TopsatenTensorWrapper t_out(out);
  EXEC_TOPSATEN_CMD(topsatenGe, self, t_out.get(), t_self.get(), t_other.get());
  return out;
}

REGISTER_IMPL_TO_DISPATCHER(GeTensorFn, ge_tensor_dispatcher, Backend::kGcu, GeTensorKernelGcu)

at::Tensor LogicalAndKernelGcu(const at::Tensor& self, const at::Tensor& other) {
  if (!gcu::TopsatenSupportsDtype(self.scalar_type()) ||
      !gcu::TopsatenSupportsDtype(other.scalar_type())) {
    return at::logical_and(self.cpu(), other.cpu()).to(self.device());
  }
  auto result_dtype = at::result_type(self, other);
  auto self_c = self.scalar_type() == result_dtype ? self : self.to(result_dtype);
  auto other_c = other.to(self.device(), result_dtype);
  auto out_shape = at::infer_size(self_c.sizes(), other_c.sizes());
  auto self_b = self_c.expand(out_shape).contiguous();
  auto other_b = other_c.expand(out_shape).contiguous();
  auto out = at::empty(out_shape, self.options().dtype(at::kBool));

  gcu::TopsatenTensorWrapper t_self(self_b);
  gcu::TopsatenTensorWrapper t_other(other_b);
  gcu::TopsatenTensorWrapper t_out(out);
  EXEC_TOPSATEN_CMD(topsatenLogicalAnd, self, t_out.get(), t_self.get(), t_other.get());
  return out;
}

REGISTER_IMPL_TO_DISPATCHER(LogicalAndFn, logical_and_dispatcher, Backend::kGcu, LogicalAndKernelGcu)

at::Tensor LogicalOrKernelGcu(const at::Tensor& self, const at::Tensor& other) {
  if (!gcu::TopsatenSupportsDtype(self.scalar_type()) ||
      !gcu::TopsatenSupportsDtype(other.scalar_type())) {
    return at::logical_or(self.cpu(), other.cpu()).to(self.device());
  }
  auto result_dtype = at::result_type(self, other);
  auto self_c = self.scalar_type() == result_dtype ? self : self.to(result_dtype);
  auto other_c = other.to(self.device(), result_dtype);
  auto out_shape = at::infer_size(self_c.sizes(), other_c.sizes());
  auto self_b = self_c.expand(out_shape).contiguous();
  auto other_b = other_c.expand(out_shape).contiguous();
  auto out = at::empty(out_shape, self.options().dtype(at::kBool));

  gcu::TopsatenTensorWrapper t_self(self_b);
  gcu::TopsatenTensorWrapper t_other(other_b);
  gcu::TopsatenTensorWrapper t_out(out);
  EXEC_TOPSATEN_CMD(topsatenLogicalOr, self, t_out.get(), t_self.get(), t_other.get());
  return out;
}

REGISTER_IMPL_TO_DISPATCHER(LogicalOrFn, logical_or_dispatcher, Backend::kGcu, LogicalOrKernelGcu)

at::Tensor PowTensorScalarKernelGcu(const at::Tensor& self, const at::Scalar& other) {
  if (!gcu::TopsatenSupportsDtype(self.scalar_type()) ||
      !gcu::TopsatenSupportsDtype(at::result_type(self, other))) {
    return at::pow(self.cpu(), other).to(self.device());
  }
  auto result_dtype = at::result_type(self, other);
  auto self_c = self.scalar_type() == result_dtype ? self : self.to(result_dtype);
  auto t_other = gcu::ToTopsatenScalar(other, result_dtype);
  auto out = at::empty(self.sizes(), self.options().dtype(result_dtype));

  gcu::TopsatenTensorWrapper t_self(self_c);
  gcu::TopsatenTensorWrapper t_out(out);
  EXEC_TOPSATEN_CMD(topsatenPow, self, t_out.get(), t_self.get(), t_other);
  return out;
}

REGISTER_IMPL_TO_DISPATCHER(PowTensorScalarFn, pow_tensor_scalar_dispatcher, Backend::kGcu, PowTensorScalarKernelGcu)

at::Tensor RemainderScalarKernelGcu(const at::Tensor& self, const at::Scalar& other) {
  if (!gcu::TopsatenSupportsDtype(self.scalar_type()) ||
      !gcu::TopsatenSupportsDtype(at::result_type(self, other))) {
    return at::remainder(self.cpu(), other).to(self.device());
  }
  auto result_dtype = at::result_type(self, other);
  auto self_c = self.scalar_type() == result_dtype ? self : self.to(result_dtype);
  auto t_other = gcu::ToTopsatenScalar(other, result_dtype);
  auto out = at::empty(self.sizes(), self.options().dtype(result_dtype));

  gcu::TopsatenTensorWrapper t_self(self_c);
  gcu::TopsatenTensorWrapper t_out(out);
  EXEC_TOPSATEN_CMD(topsatenRemainder, self, t_out.get(), t_self.get(), t_other);
  return out;
}

REGISTER_IMPL_TO_DISPATCHER(RemainderScalarFn, remainder_scalar_dispatcher, Backend::kGcu, RemainderScalarKernelGcu)

at::Tensor FmodScalarKernelGcu(const at::Tensor& self, const at::Scalar& other) {
  if (!gcu::TopsatenSupportsDtype(self.scalar_type()) ||
      !gcu::TopsatenSupportsDtype(at::result_type(self, other))) {
    return at::fmod(self.cpu(), other).to(self.device());
  }
  auto result_dtype = at::result_type(self, other);
  auto self_c = self.scalar_type() == result_dtype ? self : self.to(result_dtype);
  auto t_other = gcu::ToTopsatenScalar(other, result_dtype);
  auto out = at::empty(self.sizes(), self.options().dtype(result_dtype));

  gcu::TopsatenTensorWrapper t_self(self_c);
  gcu::TopsatenTensorWrapper t_out(out);
  EXEC_TOPSATEN_CMD(topsatenFmod, self, t_out.get(), t_self.get(), t_other);
  return out;
}

REGISTER_IMPL_TO_DISPATCHER(FmodScalarFn, fmod_scalar_dispatcher, Backend::kGcu, FmodScalarKernelGcu)

at::Tensor MulScalarKernelGcu(const at::Tensor& self, const at::Scalar& other) {
  if (!gcu::TopsatenSupportsDtype(self.scalar_type()) ||
      !gcu::TopsatenSupportsDtype(at::result_type(self, other))) {
    return at::mul(self.cpu(), other).to(self.device());
  }
  auto result_dtype = at::result_type(self, other);
  auto self_c = (self.scalar_type() == result_dtype ? self : self.to(result_dtype))
                    .contiguous();
  auto other_t = gcu::ScalarToDeviceTensor(
      other, self_c.sizes(), self_c.options());
  auto out = at::empty(self_c.sizes(), self.options().dtype(result_dtype));
  gcu::TopsatenTensorWrapper t_self(self_c);
  gcu::TopsatenTensorWrapper t_other(other_t);
  gcu::TopsatenTensorWrapper t_out(out);
  EXEC_TOPSATEN_CMD(topsatenMul, self, t_out.get(), t_self.get(), t_other.get());
  return out;
}

REGISTER_IMPL_TO_DISPATCHER(MulScalarFn, mul_scalar_dispatcher, Backend::kGcu, MulScalarKernelGcu)

at::Tensor DivScalarKernelGcu(const at::Tensor& self, const at::Scalar& other) {
  if (!gcu::TopsatenSupportsDtype(self.scalar_type()) ||
      !gcu::TopsatenSupportsDtype(at::result_type(self, other))) {
    return at::div(self.cpu(), other).to(self.device());
  }
  auto result_dtype = at::result_type(self, other);
  auto self_c = (self.scalar_type() == result_dtype ? self : self.to(result_dtype))
                    .contiguous();
  auto other_t = gcu::ScalarToDeviceTensor(
      other, self_c.sizes(), self_c.options());
  auto out = at::empty(self_c.sizes(), self.options().dtype(result_dtype));
  gcu::TopsatenTensorWrapper t_self(self_c);
  gcu::TopsatenTensorWrapper t_other(other_t);
  gcu::TopsatenTensorWrapper t_out(out);
  EXEC_TOPSATEN_CMD(topsatenDiv, self, t_out.get(), t_self.get(), t_other.get());
  return out;
}

REGISTER_IMPL_TO_DISPATCHER(DivScalarFn, div_scalar_dispatcher, Backend::kGcu, DivScalarKernelGcu)

at::Tensor AddScalarKernelGcu(const at::Tensor& self, const at::Scalar& other, const at::Scalar& alpha) {
  if (!gcu::TopsatenSupportsDtype(self.scalar_type()) ||
      !gcu::TopsatenSupportsDtype(at::result_type(self, other))) {
    return at::add(self.cpu(), other, alpha).to(self.device());
  }
  auto result_dtype = at::result_type(self, other);
  auto self_c = (self.scalar_type() == result_dtype ? self : self.to(result_dtype))
                    .contiguous();
  auto other_t = gcu::ScalarToDeviceTensor(
      other, self_c.sizes(), self_c.options());
  auto out = at::empty(self_c.sizes(), self.options().dtype(result_dtype));
  auto t_alpha = gcu::ToTopsatenScalar(alpha, result_dtype);

  gcu::TopsatenTensorWrapper t_self(self_c);
  gcu::TopsatenTensorWrapper t_other(other_t);
  gcu::TopsatenTensorWrapper t_out(out);
  EXEC_TOPSATEN_CMD(topsatenAdd, self, t_out.get(), t_self.get(), t_other.get(), t_alpha);
  return out;
}

REGISTER_IMPL_TO_DISPATCHER(AddScalarFn, add_scalar_dispatcher, Backend::kGcu, AddScalarKernelGcu)

at::Tensor SubScalarKernelGcu(const at::Tensor& self, const at::Scalar& other, const at::Scalar& alpha) {
  if (!gcu::TopsatenSupportsDtype(self.scalar_type()) ||
      !gcu::TopsatenSupportsDtype(at::result_type(self, other))) {
    return at::sub(self.cpu(), other, alpha).to(self.device());
  }
  auto result_dtype = at::result_type(self, other);
  auto self_c = (self.scalar_type() == result_dtype ? self : self.to(result_dtype))
                    .contiguous();
  auto other_t = gcu::ScalarToDeviceTensor(
      other, self_c.sizes(), self_c.options());
  auto out = at::empty(self_c.sizes(), self.options().dtype(result_dtype));
  auto t_alpha = gcu::ToTopsatenScalar(alpha, result_dtype);

  gcu::TopsatenTensorWrapper t_self(self_c);
  gcu::TopsatenTensorWrapper t_other(other_t);
  gcu::TopsatenTensorWrapper t_out(out);
  EXEC_TOPSATEN_CMD(topsatenSub, self, t_out.get(), t_self.get(), t_other.get(), t_alpha);
  return out;
}

REGISTER_IMPL_TO_DISPATCHER(SubScalarFn, sub_scalar_dispatcher, Backend::kGcu, SubScalarKernelGcu)

at::Tensor EqScalarKernelGcu(const at::Tensor& self, const at::Scalar& other) {
  if (!gcu::TopsatenSupportsDtype(self.scalar_type()) ||
      !gcu::TopsatenSupportsDtype(at::result_type(self, other))) {
    return at::eq(self.cpu(), other).to(self.device());
  }
  auto out = at::empty(self.sizes(), self.options().dtype(at::kBool));
  auto t_other = gcu::ToTopsatenScalar(other, self.scalar_type());

  gcu::TopsatenTensorWrapper t_self(self);
  gcu::TopsatenTensorWrapper t_out(out);
  EXEC_TOPSATEN_CMD(topsatenEq, self, t_out.get(), t_self.get(), t_other);
  return out;
}

REGISTER_IMPL_TO_DISPATCHER(EqScalarFn, eq_scalar_dispatcher, Backend::kGcu, EqScalarKernelGcu)

at::Tensor NeScalarKernelGcu(const at::Tensor& self, const at::Scalar& other) {
  if (!gcu::TopsatenSupportsDtype(self.scalar_type()) ||
      !gcu::TopsatenSupportsDtype(at::result_type(self, other))) {
    return at::ne(self.cpu(), other).to(self.device());
  }
  auto out = at::empty(self.sizes(), self.options().dtype(at::kBool));
  auto t_other = gcu::ToTopsatenScalar(other, self.scalar_type());

  gcu::TopsatenTensorWrapper t_self(self);
  gcu::TopsatenTensorWrapper t_out(out);
  EXEC_TOPSATEN_CMD(topsatenNe, self, t_out.get(), t_self.get(), t_other);
  return out;
}

REGISTER_IMPL_TO_DISPATCHER(NeScalarFn, ne_scalar_dispatcher, Backend::kGcu, NeScalarKernelGcu)

at::Tensor LtScalarKernelGcu(const at::Tensor& self, const at::Scalar& other) {
  if (!gcu::TopsatenSupportsDtype(self.scalar_type()) ||
      !gcu::TopsatenSupportsDtype(at::result_type(self, other))) {
    return at::lt(self.cpu(), other).to(self.device());
  }
  auto out = at::empty(self.sizes(), self.options().dtype(at::kBool));
  auto t_other = gcu::ToTopsatenScalar(other, self.scalar_type());

  gcu::TopsatenTensorWrapper t_self(self);
  gcu::TopsatenTensorWrapper t_out(out);
  EXEC_TOPSATEN_CMD(topsatenLt, self, t_out.get(), t_self.get(), t_other);
  return out;
}

REGISTER_IMPL_TO_DISPATCHER(LtScalarFn, lt_scalar_dispatcher, Backend::kGcu, LtScalarKernelGcu)

at::Tensor GtScalarKernelGcu(const at::Tensor& self, const at::Scalar& other) {
  if (!gcu::TopsatenSupportsDtype(self.scalar_type()) ||
      !gcu::TopsatenSupportsDtype(at::result_type(self, other))) {
    return at::gt(self.cpu(), other).to(self.device());
  }
  auto out = at::empty(self.sizes(), self.options().dtype(at::kBool));
  auto t_other = gcu::ToTopsatenScalar(other, self.scalar_type());

  gcu::TopsatenTensorWrapper t_self(self);
  gcu::TopsatenTensorWrapper t_out(out);
  EXEC_TOPSATEN_CMD(topsatenGt, self, t_out.get(), t_self.get(), t_other);
  return out;
}

REGISTER_IMPL_TO_DISPATCHER(GtScalarFn, gt_scalar_dispatcher, Backend::kGcu, GtScalarKernelGcu)

at::Tensor LeScalarKernelGcu(const at::Tensor& self, const at::Scalar& other) {
  if (!gcu::TopsatenSupportsDtype(self.scalar_type()) ||
      !gcu::TopsatenSupportsDtype(at::result_type(self, other))) {
    return at::le(self.cpu(), other).to(self.device());
  }
  auto out = at::empty(self.sizes(), self.options().dtype(at::kBool));
  auto t_other = gcu::ToTopsatenScalar(other, self.scalar_type());

  gcu::TopsatenTensorWrapper t_self(self);
  gcu::TopsatenTensorWrapper t_out(out);
  EXEC_TOPSATEN_CMD(topsatenLe, self, t_out.get(), t_self.get(), t_other);
  return out;
}

REGISTER_IMPL_TO_DISPATCHER(LeScalarFn, le_scalar_dispatcher, Backend::kGcu, LeScalarKernelGcu)

at::Tensor GeScalarKernelGcu(const at::Tensor& self, const at::Scalar& other) {
  if (!gcu::TopsatenSupportsDtype(self.scalar_type()) ||
      !gcu::TopsatenSupportsDtype(at::result_type(self, other))) {
    return at::ge(self.cpu(), other).to(self.device());
  }
  auto out = at::empty(self.sizes(), self.options().dtype(at::kBool));
  auto t_other = gcu::ToTopsatenScalar(other, self.scalar_type());

  gcu::TopsatenTensorWrapper t_self(self);
  gcu::TopsatenTensorWrapper t_out(out);
  EXEC_TOPSATEN_CMD(topsatenGe, self, t_out.get(), t_self.get(), t_other);
  return out;
}

REGISTER_IMPL_TO_DISPATCHER(GeScalarFn, ge_scalar_dispatcher, Backend::kGcu, GeScalarKernelGcu)

at::Tensor MmKernelGcu(const at::Tensor& self, const at::Tensor& mat2) {
  if (!gcu::TopsatenSupportsDtype(self.scalar_type()) ||
      !gcu::TopsatenSupportsDtype(mat2.scalar_type())) {
    return at::mm(self.cpu(), mat2.cpu()).to(self.device());
  }
  std::vector<int64_t> out_shape = self.sizes().vec();
  out_shape.back() = mat2.size(-1);
  auto out = at::empty(out_shape, self.options());

  gcu::TopsatenTensorWrapper t_self(self);
  gcu::TopsatenTensorWrapper t_mat2(mat2);
  gcu::TopsatenTensorWrapper t_out(out);
  EXEC_TOPSATEN_CMD(topsatenMm, self, t_out.get(), t_self.get(), t_mat2.get());
  return out;
}

REGISTER_IMPL_TO_DISPATCHER(MmFn, mm_dispatcher, Backend::kGcu, MmKernelGcu)

at::Tensor BmmKernelGcu(const at::Tensor& self, const at::Tensor& mat2) {
  if (!gcu::TopsatenSupportsDtype(self.scalar_type()) ||
      !gcu::TopsatenSupportsDtype(mat2.scalar_type())) {
    return at::bmm(self.cpu(), mat2.cpu()).to(self.device());
  }
  std::vector<int64_t> out_shape = self.sizes().vec();
  out_shape.back() = mat2.size(-1);
  auto out = at::empty(out_shape, self.options());

  gcu::TopsatenTensorWrapper t_self(self);
  gcu::TopsatenTensorWrapper t_mat2(mat2);
  gcu::TopsatenTensorWrapper t_out(out);
  EXEC_TOPSATEN_CMD(topsatenBmm, self, t_out.get(), t_self.get(), t_mat2.get());
  return out;
}

REGISTER_IMPL_TO_DISPATCHER(BmmFn, bmm_dispatcher, Backend::kGcu, BmmKernelGcu)

at::Tensor& MmOutKernelGcu(const at::Tensor& self, const at::Tensor& mat2, at::Tensor& out) {
  gcu::TopsDeviceGuard out_guard(self);
  auto result_dtype = at::result_type(self, mat2);
  TORCH_CHECK(
      c10::canCast(result_dtype, out.scalar_type()),
      "result type ",
      result_dtype,
      " can't be cast to the desired output type ",
      out.scalar_type());
  if (!gcu::TopsatenSupportsDtype(self.scalar_type()) ||
      !gcu::TopsatenSupportsDtype(mat2.scalar_type())) {
    out.copy_(at::mm(self.cpu(), mat2.cpu()));
    return out;
  }
  std::vector<int64_t> out_shape = self.sizes().vec();
  out_shape.back() = mat2.size(-1);
  if (!out.sizes().equals(out_shape)) {
    out.resize_(out_shape);
  }
  if (out.scalar_type() != result_dtype || !out.is_contiguous() ||
      out.device() != self.device()) {
    out.copy_(at::mm(self.cpu(), mat2.cpu()));
    return out;
  }

  gcu::TopsatenTensorWrapper t_self(self);
  gcu::TopsatenTensorWrapper t_mat2(mat2);
  gcu::TopsatenTensorWrapper t_out(out);
  EXEC_TOPSATEN_CMD(topsatenMm, self, t_out.get(), t_self.get(), t_mat2.get());
  return out;
}

REGISTER_IMPL_TO_DISPATCHER(MmOutFn, mm_out_dispatcher, Backend::kGcu, MmOutKernelGcu)

at::Tensor& BmmOutKernelGcu(const at::Tensor& self, const at::Tensor& mat2, at::Tensor& out) {
  gcu::TopsDeviceGuard out_guard(self);
  auto result_dtype = at::result_type(self, mat2);
  TORCH_CHECK(
      c10::canCast(result_dtype, out.scalar_type()),
      "result type ",
      result_dtype,
      " can't be cast to the desired output type ",
      out.scalar_type());
  if (!gcu::TopsatenSupportsDtype(self.scalar_type()) ||
      !gcu::TopsatenSupportsDtype(mat2.scalar_type())) {
    out.copy_(at::bmm(self.cpu(), mat2.cpu()));
    return out;
  }
  std::vector<int64_t> out_shape = self.sizes().vec();
  out_shape.back() = mat2.size(-1);
  if (!out.sizes().equals(out_shape)) {
    out.resize_(out_shape);
  }
  if (out.scalar_type() != result_dtype || !out.is_contiguous() ||
      out.device() != self.device()) {
    out.copy_(at::bmm(self.cpu(), mat2.cpu()));
    return out;
  }

  gcu::TopsatenTensorWrapper t_self(self);
  gcu::TopsatenTensorWrapper t_mat2(mat2);
  gcu::TopsatenTensorWrapper t_out(out);
  EXEC_TOPSATEN_CMD(topsatenBmm, self, t_out.get(), t_self.get(), t_mat2.get());
  return out;
}

REGISTER_IMPL_TO_DISPATCHER(BmmOutFn, bmm_out_dispatcher, Backend::kGcu, BmmOutKernelGcu)

at::Tensor SumDimIntlistKernelGcu(
    const at::Tensor& self,
    at::OptionalIntArrayRef dim,
    bool keepdim,
    ::std::optional<at::ScalarType> dtype) {
  // Integral reductions accumulate in int64 (PyTorch's rule), which topsaten
  // cannot express, so they take the CPU path along with int64 inputs.
  if (!gcu::TopsatenSupportsDtype(self.scalar_type()) ||
      (true && at::isIntegralType(self.scalar_type(), true)) ||
      (dtype.has_value() && !gcu::TopsatenSupportsDtype(dtype.value()))) {
    return at::sum(self.cpu(), dim, keepdim, dtype).to(self.device());
  }
  int64_t ndim = self.dim();
  std::vector<int64_t> norm_dims;
  if (dim.has_value() && !dim.value().empty()) {
    for (int64_t d : dim.value()) norm_dims.push_back(d < 0 ? d + ndim : d);
  } else {
    for (int64_t d = 0; d < ndim; ++d) norm_dims.push_back(d);
  }
  auto out_shape = self.sizes().vec();
  std::vector<int64_t> sorted_dims(norm_dims);
  std::sort(sorted_dims.rbegin(), sorted_dims.rend());
  for (int64_t d : sorted_dims) {
    if (keepdim) out_shape[d] = 1;
    else out_shape.erase(out_shape.begin() + d);
  }

  auto out_dtype = dtype.value_or(
      true && at::isIntegralType(self.scalar_type(), true)
          ? at::kLong
          : self.scalar_type());
  auto self_c = self.scalar_type() == out_dtype ? self : self.to(out_dtype);
  auto out = at::empty(out_shape, self.options().dtype(out_dtype));
  gcu::TopsatenSizeWrapper t_dims(norm_dims);

  gcu::TopsatenTensorWrapper t_self(self_c);
  gcu::TopsatenTensorWrapper t_out(out);
  EXEC_TOPSATEN_CMD(topsatenSum, self, t_out.get(), t_self.get(), t_dims.get(),
      keepdim, gcu::ToTopsatenDataType(out_dtype));
  return out;
}

REGISTER_IMPL_TO_DISPATCHER(SumDimIntlistFn, sum_dim_intlist_dispatcher, Backend::kGcu, SumDimIntlistKernelGcu)

at::Tensor MeanDimKernelGcu(
    const at::Tensor& self,
    at::OptionalIntArrayRef dim,
    bool keepdim,
    ::std::optional<at::ScalarType> dtype) {
  // Integral reductions accumulate in int64 (PyTorch's rule), which topsaten
  // cannot express, so they take the CPU path along with int64 inputs.
  if (!gcu::TopsatenSupportsDtype(self.scalar_type()) ||
      (false && at::isIntegralType(self.scalar_type(), true)) ||
      (dtype.has_value() && !gcu::TopsatenSupportsDtype(dtype.value()))) {
    return at::mean(self.cpu(), dim, keepdim, dtype).to(self.device());
  }
  int64_t ndim = self.dim();
  std::vector<int64_t> norm_dims;
  if (dim.has_value() && !dim.value().empty()) {
    for (int64_t d : dim.value()) norm_dims.push_back(d < 0 ? d + ndim : d);
  } else {
    for (int64_t d = 0; d < ndim; ++d) norm_dims.push_back(d);
  }
  auto out_shape = self.sizes().vec();
  std::vector<int64_t> sorted_dims(norm_dims);
  std::sort(sorted_dims.rbegin(), sorted_dims.rend());
  for (int64_t d : sorted_dims) {
    if (keepdim) out_shape[d] = 1;
    else out_shape.erase(out_shape.begin() + d);
  }

  auto out_dtype = dtype.value_or(
      false && at::isIntegralType(self.scalar_type(), true)
          ? at::kLong
          : self.scalar_type());
  auto self_c = self.scalar_type() == out_dtype ? self : self.to(out_dtype);
  auto out = at::empty(out_shape, self.options().dtype(out_dtype));
  gcu::TopsatenSizeWrapper t_dims(norm_dims);

  gcu::TopsatenTensorWrapper t_self(self_c);
  gcu::TopsatenTensorWrapper t_out(out);
  EXEC_TOPSATEN_CMD(topsatenMean, self, t_out.get(), t_self.get(), t_dims.get(),
      keepdim, gcu::ToTopsatenDataType(out_dtype));
  return out;
}

REGISTER_IMPL_TO_DISPATCHER(MeanDimFn, mean_dim_dispatcher, Backend::kGcu, MeanDimKernelGcu)

at::Tensor SumKernelGcu(
    const at::Tensor& self, ::std::optional<at::ScalarType> dtype) {
  // See T_REDUCE_DIMS_DTYPE: int64 accumulation is not available on topsaten.
  if (!gcu::TopsatenSupportsDtype(self.scalar_type()) ||
      (true && at::isIntegralType(self.scalar_type(), true)) ||
      (dtype.has_value() && !gcu::TopsatenSupportsDtype(dtype.value()))) {
    return at::sum(self.cpu(), dtype).to(self.device());
  }
  auto out_dtype = dtype.value_or(
      true && at::isIntegralType(self.scalar_type(), true)
          ? at::kLong
          : self.scalar_type());
  auto self_c = self.scalar_type() == out_dtype ? self : self.to(out_dtype);
  auto out = at::empty({}, self.options().dtype(out_dtype));

  gcu::TopsatenTensorWrapper t_self(self_c);
  gcu::TopsatenTensorWrapper t_out(out);
  EXEC_TOPSATEN_CMD(topsatenSum, self, t_out.get(), t_self.get(),
      gcu::ToTopsatenDataType(out_dtype));
  return out;
}

REGISTER_IMPL_TO_DISPATCHER(SumFn, sum_dispatcher, Backend::kGcu, SumKernelGcu)

at::Tensor MeanKernelGcu(
    const at::Tensor& self, ::std::optional<at::ScalarType> dtype) {
  // See T_REDUCE_DIMS_DTYPE: int64 accumulation is not available on topsaten.
  if (!gcu::TopsatenSupportsDtype(self.scalar_type()) ||
      (false && at::isIntegralType(self.scalar_type(), true)) ||
      (dtype.has_value() && !gcu::TopsatenSupportsDtype(dtype.value()))) {
    return at::mean(self.cpu(), dtype).to(self.device());
  }
  auto out_dtype = dtype.value_or(
      false && at::isIntegralType(self.scalar_type(), true)
          ? at::kLong
          : self.scalar_type());
  auto self_c = self.scalar_type() == out_dtype ? self : self.to(out_dtype);
  auto out = at::empty({}, self.options().dtype(out_dtype));

  gcu::TopsatenTensorWrapper t_self(self_c);
  gcu::TopsatenTensorWrapper t_out(out);
  EXEC_TOPSATEN_CMD(topsatenMean, self, t_out.get(), t_self.get(),
      gcu::ToTopsatenDataType(out_dtype));
  return out;
}

REGISTER_IMPL_TO_DISPATCHER(MeanFn, mean_dispatcher, Backend::kGcu, MeanKernelGcu)

at::Tensor AmaxKernelGcu(
    const at::Tensor& self,
    at::IntArrayRef dim,
    bool keepdim) {
  if (!gcu::TopsatenSupportsDtype(self.scalar_type())) {
    return at::amax(self.cpu(), dim, keepdim).to(self.device());
  }
  int64_t ndim = self.dim();
  std::vector<int64_t> norm_dims;
  if (!dim.empty()) {
    for (int64_t d : dim) norm_dims.push_back(d < 0 ? d + ndim : d);
  } else {
    for (int64_t d = 0; d < ndim; ++d) norm_dims.push_back(d);
  }
  auto out_shape = self.sizes().vec();
  std::vector<int64_t> sorted_dims(norm_dims);
  std::sort(sorted_dims.rbegin(), sorted_dims.rend());
  for (int64_t d : sorted_dims) {
    if (keepdim) {
      out_shape[d] = 1;
    } else {
      out_shape.erase(out_shape.begin() + d);
    }
  }
  auto out = at::empty(out_shape, self.options());

  gcu::TopsatenSizeWrapper t_dims(norm_dims);
  gcu::TopsatenTensorWrapper t_self(self);
  gcu::TopsatenTensorWrapper t_out(out);
  EXEC_TOPSATEN_CMD(
      topsatenAmax, self, t_out.get(), t_self.get(), t_dims.get(), keepdim);
  return out;
}

REGISTER_IMPL_TO_DISPATCHER(AmaxFn, amax_dispatcher, Backend::kGcu, AmaxKernelGcu)

at::Tensor AminKernelGcu(
    const at::Tensor& self,
    at::IntArrayRef dim,
    bool keepdim) {
  if (!gcu::TopsatenSupportsDtype(self.scalar_type())) {
    return at::amin(self.cpu(), dim, keepdim).to(self.device());
  }
  int64_t ndim = self.dim();
  std::vector<int64_t> norm_dims;
  if (!dim.empty()) {
    for (int64_t d : dim) norm_dims.push_back(d < 0 ? d + ndim : d);
  } else {
    for (int64_t d = 0; d < ndim; ++d) norm_dims.push_back(d);
  }
  auto out_shape = self.sizes().vec();
  std::vector<int64_t> sorted_dims(norm_dims);
  std::sort(sorted_dims.rbegin(), sorted_dims.rend());
  for (int64_t d : sorted_dims) {
    if (keepdim) {
      out_shape[d] = 1;
    } else {
      out_shape.erase(out_shape.begin() + d);
    }
  }
  auto out = at::empty(out_shape, self.options());

  gcu::TopsatenSizeWrapper t_dims(norm_dims);
  gcu::TopsatenTensorWrapper t_self(self);
  gcu::TopsatenTensorWrapper t_out(out);
  EXEC_TOPSATEN_CMD(
      topsatenAmin, self, t_out.get(), t_self.get(), t_dims.get(), keepdim);
  return out;
}

REGISTER_IMPL_TO_DISPATCHER(AminFn, amin_dispatcher, Backend::kGcu, AminKernelGcu)

at::Tensor LinalgVectorNormKernelGcu(
    const at::Tensor& self,
    const at::Scalar& ord,
    at::OptionalIntArrayRef dim,
    bool keepdim,
    ::std::optional<at::ScalarType> dtype) {
  auto out_dtype = dtype.value_or(self.scalar_type());
  if (self.scalar_type() != at::kFloat ||
      !gcu::TopsatenSupportsDtype(out_dtype) ||
      !self.is_contiguous() || self.numel() == 0) {
    return at::linalg_vector_norm(self.cpu(), ord, dim, keepdim, dtype).to(self.device());
  }
  int64_t ndim = self.dim();
  std::vector<int64_t> reduce_dims;
  if (dim.has_value() && !dim.value().empty()) {
    for (int64_t d : dim.value()) reduce_dims.push_back(d < 0 ? d + ndim : d);
  } else {
    for (int64_t d = 0; d < ndim; ++d) reduce_dims.push_back(d);
  }

  auto out_shape = self.sizes().vec();
  std::vector<int64_t> sorted_dims(reduce_dims);
  std::sort(sorted_dims.rbegin(), sorted_dims.rend());
  for (int64_t d : sorted_dims) {
    if (keepdim) out_shape[d] = 1;
    else out_shape.erase(out_shape.begin() + d);
  }
  // The buffer the vendor writes is the keepdim=true shape; out_shape is what
  // ATen promises and is reached by a (metadata-only) reshape.
  auto kept_shape = self.sizes().vec();
  for (int64_t d : reduce_dims) kept_shape[d] = 1;

  auto self_c = self.scalar_type() == out_dtype ? self : self.to(out_dtype);
  auto out = at::empty(kept_shape, self.options().dtype(out_dtype));
  gcu::TopsatenSizeWrapper t_dims(reduce_dims);
  // `ord` is a real order (2 for F.normalize, +/-inf for max/min norms), so it
  // is staged as a float whatever integral form the Python caller passed.
  auto t_ord = gcu::ToTopsatenScalar(ord, at::kFloat);

  gcu::TopsatenTensorWrapper t_self(self_c);
  gcu::TopsatenTensorWrapper t_out(out);
  EXEC_TOPSATEN_CMD(topsatenLinalgVectorNorm, self, t_out.get(), t_self.get(), t_ord, t_dims.get(),
      true, gcu::ToTopsatenDataType(out_dtype));
  return out.reshape(out_shape);
}

REGISTER_IMPL_TO_DISPATCHER(LinalgVectorNormFn, linalg_vector_norm_dispatcher, Backend::kGcu, LinalgVectorNormKernelGcu)

at::Tensor TrilKernelGcu(const at::Tensor& self, int64_t k) {
  if (!gcu::TopsatenSupportsDtype(self.scalar_type())) {
    return at::tril(self.cpu(), k).to(self.device());
  }
  auto self_c = self.contiguous();
  auto out = at::empty(self_c.sizes(), self_c.options());
  gcu::TopsatenTensorWrapper t_self(self_c);
  gcu::TopsatenTensorWrapper t_out(out);
  EXEC_TOPSATEN_CMD(topsatenTril, self, t_out.get(), t_self.get(), k);
  return out;
}

REGISTER_IMPL_TO_DISPATCHER(TrilFn, tril_dispatcher, Backend::kGcu, TrilKernelGcu)

at::Tensor TriuKernelGcu(const at::Tensor& self, int64_t k) {
  if (!gcu::TopsatenSupportsDtype(self.scalar_type())) {
    return at::triu(self.cpu(), k).to(self.device());
  }
  auto self_c = self.contiguous();
  auto out = at::empty(self_c.sizes(), self_c.options());
  gcu::TopsatenTensorWrapper t_self(self_c);
  gcu::TopsatenTensorWrapper t_out(out);
  EXEC_TOPSATEN_CMD(topsatenTriu, self, t_out.get(), t_self.get(), k);
  return out;
}

REGISTER_IMPL_TO_DISPATCHER(TriuFn, triu_dispatcher, Backend::kGcu, TriuKernelGcu)

at::Tensor FlipKernelGcu(const at::Tensor& self, at::IntArrayRef dims) {
  if (!gcu::TopsatenSupportsDtype(self.scalar_type())) {
    return at::flip(self.cpu(), dims).to(self.device());
  }
  int64_t ndim = self.dim();
  std::vector<int64_t> norm_dims;
  for (int64_t d : dims) norm_dims.push_back(d < 0 ? d + ndim : d);
  auto self_c = self.contiguous();
  auto out = at::empty(self_c.sizes(), self_c.options());

  gcu::TopsatenSizeWrapper t_dims(norm_dims);
  gcu::TopsatenTensorWrapper t_self(self_c);
  gcu::TopsatenTensorWrapper t_out(out);
  EXEC_TOPSATEN_CMD(topsatenFlip, self, t_out.get(), t_self.get(), t_dims.get());
  return out;
}

REGISTER_IMPL_TO_DISPATCHER(FlipFn, flip_dispatcher, Backend::kGcu, FlipKernelGcu)

at::Tensor GeluKernelGcu(const at::Tensor& self, c10::string_view approximate) {
  if (!gcu::TopsatenSupportsDtype(self.scalar_type())) {
    return at::gelu(self.cpu(), approximate).to(self.device());
  }
  auto out = at::empty(self.sizes(), self.options());
  std::string approx(approximate);

  gcu::TopsatenTensorWrapper t_self(self);
  gcu::TopsatenTensorWrapper t_out(out);
  EXEC_TOPSATEN_CMD(topsatenGelu, self, t_out.get(), t_self.get(), approx.c_str());
  return out;
}

REGISTER_IMPL_TO_DISPATCHER(GeluFn, gelu_dispatcher, Backend::kGcu, GeluKernelGcu)

at::Tensor PrivSoftmaxKernelGcu(const at::Tensor& self, int64_t dim, bool half_to_float) {
  if (!gcu::TopsatenSupportsDtype(self.scalar_type())) {
    return at::_softmax(self.cpu(), dim, half_to_float).to(self.device());
  }
  int64_t d = dim < 0 ? dim + self.dim() : dim;
  auto out_dtype = half_to_float ? at::kFloat : self.scalar_type();
  auto out = at::empty(self.sizes(), self.options().dtype(out_dtype));

  gcu::TopsatenTensorWrapper t_self(self);
  gcu::TopsatenTensorWrapper t_out(out);
  EXEC_TOPSATEN_CMD(topsatenSoftmaxForward, self, t_out.get(), t_self.get(), d, half_to_float);
  return out;
}

REGISTER_IMPL_TO_DISPATCHER(PrivSoftmaxFn, priv_softmax_dispatcher, Backend::kGcu, PrivSoftmaxKernelGcu)

at::Tensor ClampKernelGcu(
    const at::Tensor& self,
    const ::std::optional<at::Scalar>& min,
    const ::std::optional<at::Scalar>& max) {
  if (!gcu::TopsatenSupportsDtype(self.scalar_type())) {
    return at::clamp(self.cpu(), min, max).to(self.device());
  }
  auto self_c = self.contiguous();
  auto out = at::empty(self_c.sizes(), self_c.options());
  auto dtype = self.scalar_type();
  auto lo = min.has_value()
      ? gcu::ToTopsatenScalar(min.value(), dtype)
      : gcu::ToTopsatenScalar(gcu::DtypeLowest(dtype), dtype);
  auto hi = max.has_value()
      ? gcu::ToTopsatenScalar(max.value(), dtype)
      : gcu::ToTopsatenScalar(gcu::DtypeHighest(dtype), dtype);

  gcu::TopsatenTensorWrapper t_self(self_c);
  gcu::TopsatenTensorWrapper t_out(out);
  EXEC_TOPSATEN_CMD(topsatenClamp, self, t_out.get(), t_self.get(), lo, hi);
  return out;
}

REGISTER_IMPL_TO_DISPATCHER(ClampFn, clamp_dispatcher, Backend::kGcu, ClampKernelGcu)

at::Tensor AddmmKernelGcu(
    const at::Tensor& self,
    const at::Tensor& mat1,
    const at::Tensor& mat2,
    const at::Scalar& beta,
    const at::Scalar& alpha) {
  if (!gcu::TopsatenSupportsDtype(self.scalar_type()) ||
      !gcu::TopsatenSupportsDtype(mat1.scalar_type()) ||
      !gcu::TopsatenSupportsDtype(mat2.scalar_type())) {
    return at::addmm(self.cpu(), mat1.cpu(), mat2.cpu(), beta, alpha)
        .to(self.device());
  }
  std::vector<int64_t> out_shape{mat1.size(0), mat2.size(1)};
  auto self_b = self.expand(out_shape).contiguous();
  auto mat1_c = mat1.contiguous();
  auto mat2_c = mat2.contiguous();
  auto t_beta = gcu::ToTopsatenScalar(beta, self.scalar_type());
  auto t_alpha = gcu::ToTopsatenScalar(alpha, self.scalar_type());
  auto out = at::empty(out_shape, self.options());

  gcu::TopsatenTensorWrapper t_self(self_b);
  gcu::TopsatenTensorWrapper t_mat1(mat1_c);
  gcu::TopsatenTensorWrapper t_mat2(mat2_c);
  gcu::TopsatenTensorWrapper t_out(out);
  EXEC_TOPSATEN_CMD(
      topsatenAddmm, self, t_out.get(), t_self.get(), t_mat1.get(), t_mat2.get(),
      t_beta, t_alpha);
  return out;
}

REGISTER_IMPL_TO_DISPATCHER(AddmmFn, addmm_dispatcher, Backend::kGcu, AddmmKernelGcu)

at::Tensor& AddmmOutKernelGcu(
    const at::Tensor& self,
    const at::Tensor& mat1,
    const at::Tensor& mat2,
    const at::Scalar& beta,
    const at::Scalar& alpha,
    at::Tensor& out) {
  gcu::TopsDeviceGuard out_guard(self);
  auto result_dtype = c10::promoteTypes(at::result_type(self, mat1), mat2.scalar_type());
  TORCH_CHECK(
      c10::canCast(result_dtype, out.scalar_type()),
      "result type ",
      result_dtype,
      " can't be cast to the desired output type ",
      out.scalar_type());
  if (!gcu::TopsatenSupportsDtype(self.scalar_type()) ||
      !gcu::TopsatenSupportsDtype(mat1.scalar_type()) ||
      !gcu::TopsatenSupportsDtype(mat2.scalar_type())) {
    out.copy_(at::addmm(self.cpu(), mat1.cpu(), mat2.cpu(), beta, alpha));
    return out;
  }
  std::vector<int64_t> out_shape{mat1.size(0), mat2.size(1)};
  auto self_b = self.expand(out_shape).contiguous();
  auto mat1_c = mat1.contiguous();
  auto mat2_c = mat2.contiguous();
  auto t_beta = gcu::ToTopsatenScalar(beta, self.scalar_type());
  auto t_alpha = gcu::ToTopsatenScalar(alpha, self.scalar_type());
  if (!out.sizes().equals(out_shape)) {
    out.resize_(out_shape);
  }
  if (out.scalar_type() != result_dtype || !out.is_contiguous() ||
      out.device() != self.device()) {
    out.copy_(at::addmm(self.cpu(), mat1.cpu(), mat2.cpu(), beta, alpha));
    return out;
  }

  gcu::TopsatenTensorWrapper t_self(self_b);
  gcu::TopsatenTensorWrapper t_mat1(mat1_c);
  gcu::TopsatenTensorWrapper t_mat2(mat2_c);
  gcu::TopsatenTensorWrapper t_out(out);
  EXEC_TOPSATEN_CMD(
      topsatenAddmm, self, t_out.get(), t_self.get(), t_mat1.get(), t_mat2.get(),
      t_beta, t_alpha);
  return out;
}

REGISTER_IMPL_TO_DISPATCHER(AddmmOutFn, addmm_out_dispatcher, Backend::kGcu, AddmmOutKernelGcu)

at::Tensor CatKernelGcu(const at::ITensorListRef& tensors, int64_t dim) {
  std::vector<at::Tensor> inputs;
  bool cpu_path = false;
  for (const at::Tensor& t : tensors) {
    if (t.numel() == 0 && t.dim() == 1) continue;
    if (!gcu::TopsatenSupportsDtype(t.scalar_type())) cpu_path = true;
    inputs.push_back(t);
  }
  TORCH_CHECK(!inputs.empty(), "flagos cat: expected at least one tensor");
  if (cpu_path) {
    std::vector<at::Tensor> host;
    host.reserve(inputs.size());
    for (const auto& t : inputs) host.push_back(t.cpu());
    return at::cat(host, dim).to(inputs[0].device());
  }
  int64_t ndim = inputs[0].dim();
  int64_t d = dim < 0 ? dim + ndim : dim;

  auto out_shape = inputs[0].sizes().vec();
  int64_t total = 0;
  for (const auto& t : inputs) total += t.size(d);
  out_shape[d] = total;
  auto out = at::empty(out_shape, inputs[0].options());

  std::vector<at::Tensor> contig;
  contig.reserve(inputs.size());
  std::vector<std::unique_ptr<gcu::TopsatenTensorWrapper>> keep;
  keep.reserve(inputs.size());
  std::vector<topsatenTensor> tops_in;
  tops_in.reserve(inputs.size());
  for (const auto& t : inputs) {
    contig.push_back(t.contiguous());
    keep.push_back(
        std::make_unique<gcu::TopsatenTensorWrapper>(contig.back()));
    tops_in.push_back(keep.back()->get());
  }
  gcu::TopsatenTensorWrapper t_out(out);
  EXEC_TOPSATEN_CMD(topsatenCat, inputs[0], t_out.get(), tops_in, d);
  return out;
}

REGISTER_IMPL_TO_DISPATCHER(CatFn, cat_dispatcher, Backend::kGcu, CatKernelGcu)

at::Tensor ZerosLikeKernelGcu(
    const at::Tensor& self,
    ::std::optional<at::ScalarType> dtype,
    ::std::optional<at::Layout> layout,
    ::std::optional<at::Device> device,
    ::std::optional<bool> pin_memory,
    ::std::optional<at::MemoryFormat> memory_format) {
  auto out_dtype = dtype.value_or(self.scalar_type());
  auto target_device = device.value_or(self.device());
  if (target_device != self.device() ||
      !gcu::TopsatenSupportsDtype(out_dtype) ||
      !gcu::TopsatenSupportsDtype(self.scalar_type())) {
    // Built on a CPU tensor so the call lands on the CPU kernel: passing the
    // flagos tensor back to at::zeros_like would re-enter this kernel.
    auto host = at::zeros_like(
        self.cpu(), out_dtype, layout, at::kCPU, pin_memory, memory_format);
    return target_device.type() == at::kCPU ? host : host.to(target_device);
  }
  // at::empty rejects MemoryFormat::Preserve (autograd passes it for every
  // seed gradient), so resolve it against the input first. A non-contiguous
  // result would leave topsaten writing through strides it does not honour for
  // this op, so those go to the host path.
  auto fmt = memory_format.value_or(at::MemoryFormat::Contiguous);
  if (fmt == at::MemoryFormat::Preserve) {
    fmt = self.suggest_memory_format();
  }
  if (fmt != at::MemoryFormat::Contiguous) {
    auto host = at::zeros_like(
        self.cpu(), out_dtype, layout, at::kCPU, pin_memory, memory_format);
    return host.to(target_device);
  }
  auto options = self.options().dtype(out_dtype);
  auto out = at::empty(self.sizes(), options, fmt);

  auto self_c = self.contiguous();
  gcu::TopsatenTensorWrapper t_self(self_c);
  gcu::TopsatenTensorWrapper t_out(out);
  EXEC_TOPSATEN_CMD(
      topsatenZerosLike, self, t_out.get(), t_self.get(),
      gcu::ToTopsatenDataType(out_dtype), TOPSATEN_LAYOUT_STRIDED,
      TOPSATEN_MEMORY_CONTIGUOUS);
  return out;
}

REGISTER_IMPL_TO_DISPATCHER(ZerosLikeFn, zeros_like_dispatcher, Backend::kGcu, ZerosLikeKernelGcu)

at::Tensor OnesLikeKernelGcu(
    const at::Tensor& self,
    ::std::optional<at::ScalarType> dtype,
    ::std::optional<at::Layout> layout,
    ::std::optional<at::Device> device,
    ::std::optional<bool> pin_memory,
    ::std::optional<at::MemoryFormat> memory_format) {
  auto out_dtype = dtype.value_or(self.scalar_type());
  auto target_device = device.value_or(self.device());
  if (target_device != self.device() ||
      !gcu::TopsatenSupportsDtype(out_dtype) ||
      !gcu::TopsatenSupportsDtype(self.scalar_type())) {
    // Built on a CPU tensor so the call lands on the CPU kernel: passing the
    // flagos tensor back to at::ones_like would re-enter this kernel.
    auto host = at::ones_like(
        self.cpu(), out_dtype, layout, at::kCPU, pin_memory, memory_format);
    return target_device.type() == at::kCPU ? host : host.to(target_device);
  }
  // at::empty rejects MemoryFormat::Preserve (autograd passes it for every
  // seed gradient), so resolve it against the input first. A non-contiguous
  // result would leave topsaten writing through strides it does not honour for
  // this op, so those go to the host path.
  auto fmt = memory_format.value_or(at::MemoryFormat::Contiguous);
  if (fmt == at::MemoryFormat::Preserve) {
    fmt = self.suggest_memory_format();
  }
  if (fmt != at::MemoryFormat::Contiguous) {
    auto host = at::ones_like(
        self.cpu(), out_dtype, layout, at::kCPU, pin_memory, memory_format);
    return host.to(target_device);
  }
  auto options = self.options().dtype(out_dtype);
  auto out = at::empty(self.sizes(), options, fmt);

  auto self_c = self.contiguous();
  gcu::TopsatenTensorWrapper t_self(self_c);
  gcu::TopsatenTensorWrapper t_out(out);
  EXEC_TOPSATEN_CMD(
      topsatenOnesLike, self, t_out.get(), t_self.get(),
      gcu::ToTopsatenDataType(out_dtype), TOPSATEN_LAYOUT_STRIDED,
      TOPSATEN_MEMORY_CONTIGUOUS);
  return out;
}

REGISTER_IMPL_TO_DISPATCHER(OnesLikeFn, ones_like_dispatcher, Backend::kGcu, OnesLikeKernelGcu)

at::Tensor ArangeKernelGcu(
    const at::Scalar& end,
    ::std::optional<at::ScalarType> dtype, ::std::optional<at::Layout> layout,
    ::std::optional<at::Device> device, ::std::optional<bool> pin_memory) {
  at::Scalar start(0), step(1);
  auto out_dtype = dtype.value_or(end.isFloatingPoint()
      ? at::typeMetaToScalarType(at::get_default_dtype())
      : at::kLong);
  // An absent (or index-less) device means the current one, exactly as
  // csrc/aten/backends/flagos/python_op_caller.cc resolves it for the other
  // factories: naming index 0 here would allocate on device 0 while the op runs
  // on the current device, which on GCU is a silent cross-device write.
  auto target = (device.has_value() && device->has_index())
      ? *device
      : at::Device(at::kPrivateUse1, static_cast<int>(c10::flagos::CurrentDevice()));
  TORCH_CHECK(
      target.is_privateuseone(), "arange on GCU requires a flagos device, got ", target);
  // Same contract as at::empty on a device (csrc/aten/empty.cc).
  TORCH_CHECK(
      !pin_memory.value_or(false), "Pin memory can only be on CPU");

  auto length = out_dtype == at::kLong
      ? at::native::compute_arange_size<int64_t>(start, end, step)
      : at::native::compute_arange_size<double>(start, end, step);

  if (!gcu::TopsatenArangeDtype(out_dtype) ||
      layout.value_or(at::kStrided) != at::kStrided || length == 0) {
    // Built with at::kCPU and the caller's layout, so the call lands on the CPU
    // kernel (which re-raises for a non-strided layout the same way ATen does)
    // instead of re-entering this one.
    auto host = at::arange(start, end, step, out_dtype, layout, at::kCPU, false);
    return host.to(target);
  }

  auto out = at::empty(
      {length},
      at::TensorOptions().dtype(out_dtype).device(target).pinned_memory(false));
  auto t_start = gcu::ToTopsatenScalar(start, out_dtype);
  auto t_end = gcu::ToTopsatenScalar(end, out_dtype);
  auto t_step = gcu::ToTopsatenScalar(step, out_dtype);
  gcu::TopsatenTensorWrapper t_out(out);
  EXEC_TOPSATEN_CMD(
      topsatenArange, out, t_out.get(), t_start, t_end, t_step,
      gcu::ToTopsatenDataType(out_dtype), TOPSATEN_LAYOUT_STRIDED, false);
  return out;
}

REGISTER_IMPL_TO_DISPATCHER(ArangeFn, arange_dispatcher, Backend::kGcu, ArangeKernelGcu)

at::Tensor ArangeStartKernelGcu(
    const at::Scalar& start, const at::Scalar& end,
    ::std::optional<at::ScalarType> dtype, ::std::optional<at::Layout> layout,
    ::std::optional<at::Device> device, ::std::optional<bool> pin_memory) {
  at::Scalar step(1);
  auto out_dtype = dtype.value_or(start.isFloatingPoint() || end.isFloatingPoint()
      ? at::typeMetaToScalarType(at::get_default_dtype())
      : at::kLong);
  // An absent (or index-less) device means the current one, exactly as
  // csrc/aten/backends/flagos/python_op_caller.cc resolves it for the other
  // factories: naming index 0 here would allocate on device 0 while the op runs
  // on the current device, which on GCU is a silent cross-device write.
  auto target = (device.has_value() && device->has_index())
      ? *device
      : at::Device(at::kPrivateUse1, static_cast<int>(c10::flagos::CurrentDevice()));
  TORCH_CHECK(
      target.is_privateuseone(), "arange on GCU requires a flagos device, got ", target);
  // Same contract as at::empty on a device (csrc/aten/empty.cc).
  TORCH_CHECK(
      !pin_memory.value_or(false), "Pin memory can only be on CPU");

  auto length = out_dtype == at::kLong
      ? at::native::compute_arange_size<int64_t>(start, end, step)
      : at::native::compute_arange_size<double>(start, end, step);

  if (!gcu::TopsatenArangeDtype(out_dtype) ||
      layout.value_or(at::kStrided) != at::kStrided || length == 0) {
    // Built with at::kCPU and the caller's layout, so the call lands on the CPU
    // kernel (which re-raises for a non-strided layout the same way ATen does)
    // instead of re-entering this one.
    auto host = at::arange(start, end, step, out_dtype, layout, at::kCPU, false);
    return host.to(target);
  }

  auto out = at::empty(
      {length},
      at::TensorOptions().dtype(out_dtype).device(target).pinned_memory(false));
  auto t_start = gcu::ToTopsatenScalar(start, out_dtype);
  auto t_end = gcu::ToTopsatenScalar(end, out_dtype);
  auto t_step = gcu::ToTopsatenScalar(step, out_dtype);
  gcu::TopsatenTensorWrapper t_out(out);
  EXEC_TOPSATEN_CMD(
      topsatenArange, out, t_out.get(), t_start, t_end, t_step,
      gcu::ToTopsatenDataType(out_dtype), TOPSATEN_LAYOUT_STRIDED, false);
  return out;
}

REGISTER_IMPL_TO_DISPATCHER(ArangeStartFn, arange_start_dispatcher, Backend::kGcu, ArangeStartKernelGcu)

at::Tensor ArangeStartStepKernelGcu(
    const at::Scalar& start, const at::Scalar& end, const at::Scalar& step,
    ::std::optional<at::ScalarType> dtype, ::std::optional<at::Layout> layout,
    ::std::optional<at::Device> device, ::std::optional<bool> pin_memory) {
  
  auto out_dtype = dtype.value_or(start.isFloatingPoint() || end.isFloatingPoint() || step.isFloatingPoint()
      ? at::typeMetaToScalarType(at::get_default_dtype())
      : at::kLong);
  // An absent (or index-less) device means the current one, exactly as
  // csrc/aten/backends/flagos/python_op_caller.cc resolves it for the other
  // factories: naming index 0 here would allocate on device 0 while the op runs
  // on the current device, which on GCU is a silent cross-device write.
  auto target = (device.has_value() && device->has_index())
      ? *device
      : at::Device(at::kPrivateUse1, static_cast<int>(c10::flagos::CurrentDevice()));
  TORCH_CHECK(
      target.is_privateuseone(), "arange on GCU requires a flagos device, got ", target);
  // Same contract as at::empty on a device (csrc/aten/empty.cc).
  TORCH_CHECK(
      !pin_memory.value_or(false), "Pin memory can only be on CPU");

  auto length = out_dtype == at::kLong
      ? at::native::compute_arange_size<int64_t>(start, end, step)
      : at::native::compute_arange_size<double>(start, end, step);

  if (!gcu::TopsatenArangeDtype(out_dtype) ||
      layout.value_or(at::kStrided) != at::kStrided || length == 0) {
    // Built with at::kCPU and the caller's layout, so the call lands on the CPU
    // kernel (which re-raises for a non-strided layout the same way ATen does)
    // instead of re-entering this one.
    auto host = at::arange(start, end, step, out_dtype, layout, at::kCPU, false);
    return host.to(target);
  }

  auto out = at::empty(
      {length},
      at::TensorOptions().dtype(out_dtype).device(target).pinned_memory(false));
  auto t_start = gcu::ToTopsatenScalar(start, out_dtype);
  auto t_end = gcu::ToTopsatenScalar(end, out_dtype);
  auto t_step = gcu::ToTopsatenScalar(step, out_dtype);
  gcu::TopsatenTensorWrapper t_out(out);
  EXEC_TOPSATEN_CMD(
      topsatenArange, out, t_out.get(), t_start, t_end, t_step,
      gcu::ToTopsatenDataType(out_dtype), TOPSATEN_LAYOUT_STRIDED, false);
  return out;
}

REGISTER_IMPL_TO_DISPATCHER(ArangeStartStepFn, arange_start_step_dispatcher, Backend::kGcu, ArangeStartStepKernelGcu)

at::Tensor& ZeroInplaceKernelGcu(at::Tensor& self) {
  if (self.numel() == 0) {
    return self;
  }
  if (!gcu::TopsatenSupportsDtype(self.scalar_type()) ||
      !self.is_contiguous()) {
    auto host = self.cpu();
    host.zero_();
    self.copy_(host);
    return self;
  }
  gcu::TopsatenTensorWrapper t_self(self);
  EXEC_TOPSATEN_CMD(topsatenZero, self, t_self.get());
  return self;
}

REGISTER_IMPL_TO_DISPATCHER(ZeroInplaceFn, zero_inplace_dispatcher, Backend::kGcu, ZeroInplaceKernelGcu)

at::Tensor& FillInplaceScalarKernelGcu(at::Tensor& self, const at::Scalar& value) {
  if (self.numel() == 0) {
    return self;
  }
  if (!gcu::TopsatenSupportsDtype(self.scalar_type()) ||
      !self.is_contiguous()) {
    auto host = self.cpu();
    host.fill_(value);
    self.copy_(host);
    return self;
  }
  auto t_value = gcu::ToTopsatenScalar(value, self.scalar_type());

  gcu::TopsatenTensorWrapper t_self(self);
  EXEC_TOPSATEN_CMD(topsatenFill_, self, t_self.get(), t_value);
  return self;
}

REGISTER_IMPL_TO_DISPATCHER(FillInplaceScalarFn, fill_inplace_scalar_dispatcher, Backend::kGcu, FillInplaceScalarKernelGcu)

at::Tensor& FillInplaceTensorKernelGcu(at::Tensor& self, const at::Tensor& value) {
  if (self.numel() == 0) {
    return self;
  }
  if (!gcu::TopsatenSupportsDtype(self.scalar_type()) ||
      !self.is_contiguous() || value.numel() != 1) {
    auto host = self.cpu();
    host.fill_(value.cpu());
    self.copy_(host);
    return self;
  }
  auto value_c = value.to(self.device(), self.scalar_type()).reshape({1});

  gcu::TopsatenTensorWrapper t_self(self);
  gcu::TopsatenTensorWrapper t_value(value_c);
  EXEC_TOPSATEN_CMD(topsatenFill_, self, t_self.get(), t_value.get());
  return self;
}

REGISTER_IMPL_TO_DISPATCHER(FillInplaceTensorFn, fill_inplace_tensor_dispatcher, Backend::kGcu, FillInplaceTensorKernelGcu)

at::Tensor& MaskedFillInplaceScalarKernelGcu(at::Tensor& self, const at::Tensor& mask, const at::Scalar& value) {
  if (self.numel() == 0) {
    return self;
  }
  if (!gcu::TopsatenSupportsDtype(self.scalar_type()) ||
      !self.is_contiguous() || mask.scalar_type() != at::kBool) {
    auto host = self.cpu();
    host.masked_fill_(mask.cpu(), value);
    self.copy_(host);
    return self;
  }
  auto mask_c = mask.to(self.device()).contiguous();
  auto t_value = gcu::ToTopsatenScalar(value, self.scalar_type());

  gcu::TopsatenTensorWrapper t_self(self);
  gcu::TopsatenTensorWrapper t_mask(mask_c);
  EXEC_TOPSATEN_CMD(topsatenMasked_fill, self, t_self.get(), t_self.get(), t_mask.get(), t_value);
  return self;
}

REGISTER_IMPL_TO_DISPATCHER(MaskedFillInplaceScalarFn, masked_fill_inplace_scalar_dispatcher, Backend::kGcu, MaskedFillInplaceScalarKernelGcu)

at::Tensor& MaskedFillInplaceTensorKernelGcu(at::Tensor& self, const at::Tensor& mask, const at::Tensor& value) {
  if (self.numel() == 0) {
    return self;
  }
  if (!gcu::TopsatenSupportsDtype(self.scalar_type()) ||
      !self.is_contiguous() || mask.scalar_type() != at::kBool ||
      value.numel() != 1) {
    auto host = self.cpu();
    host.masked_fill_(mask.cpu(), value.cpu());
    self.copy_(host);
    return self;
  }
  auto mask_c = mask.to(self.device()).contiguous();
  auto value_c = value.to(self.device(), self.scalar_type()).reshape({1});

  gcu::TopsatenTensorWrapper t_self(self);
  gcu::TopsatenTensorWrapper t_mask(mask_c);
  gcu::TopsatenTensorWrapper t_value(value_c);
  EXEC_TOPSATEN_CMD(topsatenMasked_fill, self, t_self.get(), t_self.get(), t_mask.get(), t_value.get());
  return self;
}

REGISTER_IMPL_TO_DISPATCHER(MaskedFillInplaceTensorFn, masked_fill_inplace_tensor_dispatcher, Backend::kGcu, MaskedFillInplaceTensorKernelGcu)

::std::tuple<at::Tensor, at::Tensor, at::Tensor> NativeLayerNormKernelGcu(
    const at::Tensor& input,
    at::IntArrayRef normalized_shape,
    const ::std::optional<at::Tensor>& weight,
    const ::std::optional<at::Tensor>& bias,
    double eps) {
  if (!gcu::TopsatenSupportsDtype(input.scalar_type())) {
    auto r = at::native_layer_norm(
        input.cpu(),
        normalized_shape,
        weight.has_value() ? ::std::optional<at::Tensor>(weight->cpu())
                           : ::std::nullopt,
        bias.has_value() ? ::std::optional<at::Tensor>(bias->cpu())
                         : ::std::nullopt,
        eps);
    return {std::get<0>(r).to(input.device()),
            std::get<1>(r).to(input.device()),
            std::get<2>(r).to(input.device())};
  }
  auto input_c = input.contiguous();
  int64_t norm_ndim = static_cast<int64_t>(normalized_shape.size());
  int64_t outer = input_c.dim() - norm_ndim;

  auto stat_shape = input_c.sizes().vec();
  for (int64_t i = outer; i < input_c.dim(); ++i) stat_shape[i] = 1;

  auto out = at::empty(input_c.sizes(), input_c.options());
  auto mean = at::empty(stat_shape, input_c.options());
  auto rstd = at::empty(stat_shape, input_c.options());

  auto w = weight.has_value() && weight->defined()
      ? weight->contiguous()
      : at::ones(normalized_shape, input_c.options());
  auto b = bias.has_value() && bias->defined()
      ? bias->contiguous()
      : at::zeros(normalized_shape, input_c.options());

  gcu::TopsatenSizeWrapper t_shape(normalized_shape);
  gcu::TopsatenTensorWrapper t_in(input_c);
  gcu::TopsatenTensorWrapper t_w(w);
  gcu::TopsatenTensorWrapper t_b(b);
  gcu::TopsatenTensorWrapper t_out(out);
  gcu::TopsatenTensorWrapper t_mean(mean);
  gcu::TopsatenTensorWrapper t_rstd(rstd);
  auto t_eps = gcu::ToTopsatenScalar(at::Scalar(eps), input.scalar_type());
  EXEC_TOPSATEN_CMD(
      topsatenNativeLayerNorm, input, t_out.get(), t_mean.get(), t_rstd.get(), t_in.get(),
      t_shape.get(), t_w.get(), t_b.get(), t_eps);

  // PyTorch's mean/rstd drop the normalized dims entirely.
  std::vector<int64_t> pt_stat(
      input_c.sizes().begin(), input_c.sizes().begin() + outer);
  pt_stat.push_back(1);
  return {out, mean.reshape(pt_stat), rstd.reshape(pt_stat)};
}

REGISTER_IMPL_TO_DISPATCHER(NativeLayerNormFn, native_layer_norm_dispatcher, Backend::kGcu, NativeLayerNormKernelGcu)

at::Tensor PrivSoftmaxBackwardDataKernelGcu(
    const at::Tensor& grad_output,
    const at::Tensor& output,
    int64_t dim,
    at::ScalarType input_dtype) {
  if (!gcu::TopsatenSupportsDtype(grad_output.scalar_type()) ||
      !gcu::TopsatenSupportsDtype(input_dtype)) {
    return at::_softmax_backward_data(grad_output.cpu(), output.cpu(), dim, input_dtype)
        .to(grad_output.device());
  }
  int64_t d = dim < 0 ? dim + output.dim() : dim;
  auto grad_c = grad_output.contiguous();
  auto out_c = output.contiguous();
  auto result = at::empty(out_c.sizes(), out_c.options().dtype(input_dtype));

  gcu::TopsatenTensorWrapper t_grad(grad_c);
  gcu::TopsatenTensorWrapper t_out(out_c);
  gcu::TopsatenTensorWrapper t_result(result);
  EXEC_TOPSATEN_CMD(
      topsatenSoftmaxBackwardData, grad_output, t_result.get(), t_grad.get(), t_out.get(), d,
      gcu::ToTopsatenDataType(input_dtype));
  return result;
}

REGISTER_IMPL_TO_DISPATCHER(PrivSoftmaxBackwardDataFn, priv_softmax_backward_data_dispatcher, Backend::kGcu, PrivSoftmaxBackwardDataKernelGcu)

at::Tensor SiluBackwardKernelGcu(
    const at::Tensor& grad_output,
    const at::Tensor& self) {
  if (!gcu::TopsatenSupportsDtype(self.scalar_type()) ||
      !gcu::TopsatenSupportsDtype(grad_output.scalar_type())) {
    return at::silu_backward(grad_output.cpu(), self.cpu()).to(self.device());
  }
  auto grad_c = grad_output.contiguous();
  auto self_c = self.contiguous();
  auto grad_input = at::empty(self_c.sizes(), self_c.options());

  gcu::TopsatenTensorWrapper t_grad(grad_c);
  gcu::TopsatenTensorWrapper t_self(self_c);
  gcu::TopsatenTensorWrapper t_gi(grad_input);
  EXEC_TOPSATEN_CMD(topsatenSiluBackward, self, t_gi.get(), t_grad.get(), t_self.get());
  return grad_input;
}

REGISTER_IMPL_TO_DISPATCHER(SiluBackwardFn, silu_backward_dispatcher, Backend::kGcu, SiluBackwardKernelGcu)

at::Tensor MseLossKernelGcu(
    const at::Tensor& self,
    const at::Tensor& target,
    int64_t reduction) {
  if (!gcu::TopsatenSupportsDtype(self.scalar_type()) ||
      !gcu::TopsatenSupportsDtype(target.scalar_type())) {
    return at::mse_loss(self.cpu(), target.cpu(), reduction).to(self.device());
  }
  auto self_c = self.contiguous();
  auto target_c = target.to(self.device()).contiguous();
  auto out = reduction == 0
      ? at::empty(self_c.sizes(), self_c.options())
      : at::empty({}, self_c.options());

  gcu::TopsatenTensorWrapper t_self(self_c);
  gcu::TopsatenTensorWrapper t_target(target_c);
  gcu::TopsatenTensorWrapper t_out(out);
  EXEC_TOPSATEN_CMD(
      topsatenMseLoss, self, t_out.get(), t_self.get(), t_target.get(), reduction);
  return out;
}

REGISTER_IMPL_TO_DISPATCHER(MseLossFn, mse_loss_dispatcher, Backend::kGcu, MseLossKernelGcu)

at::Tensor MseLossBackwardKernelGcu(
    const at::Tensor& grad_output,
    const at::Tensor& self,
    const at::Tensor& target,
    int64_t reduction) {
  if (!gcu::TopsatenSupportsDtype(self.scalar_type()) ||
      !gcu::TopsatenSupportsDtype(target.scalar_type())) {
    return at::mse_loss_backward(
               grad_output.cpu(), self.cpu(), target.cpu(), reduction)
        .to(self.device());
  }
  auto self_c = self.contiguous();
  auto target_c = target.to(self.device()).contiguous();
  // topsaten does not broadcast the (scalar) grad for a reduced loss.
  auto grad_c = grad_output.to(self.device())
                    .expand(self_c.sizes())
                    .contiguous();
  auto grad_input = at::empty(self_c.sizes(), self_c.options());

  gcu::TopsatenTensorWrapper t_grad(grad_c);
  gcu::TopsatenTensorWrapper t_self(self_c);
  gcu::TopsatenTensorWrapper t_target(target_c);
  gcu::TopsatenTensorWrapper t_gi(grad_input);
  EXEC_TOPSATEN_CMD(
      topsatenMseLossBackward, self, t_gi.get(), t_grad.get(), t_self.get(), t_target.get(),
      reduction);
  return grad_input;
}

REGISTER_IMPL_TO_DISPATCHER(MseLossBackwardFn, mse_loss_backward_dispatcher, Backend::kGcu, MseLossBackwardKernelGcu)

void PrivAmpForeachNonFiniteCheckAndUnscaleInplaceKernelGcu(at::TensorList self, at::Tensor& found_inf, const at::Tensor& inv_scale) {
  if (self.empty() || !gcu::IsForeachEligible(self) ||
      !found_inf.defined() || !inv_scale.defined() ||
      !found_inf.is_contiguous() || !inv_scale.is_contiguous() ||
      found_inf.device() != self[0].device() ||
      inv_scale.device() != self[0].device() ||
      !gcu::TopsatenSupportsDtype(found_inf.scalar_type()) ||
      !gcu::TopsatenSupportsDtype(inv_scale.scalar_type())) {
    std::vector<at::Tensor> cpu_self;
    cpu_self.reserve(self.size());
    for (const auto& tensor : self) cpu_self.push_back(tensor.cpu());
    auto cpu_found_inf = found_inf.cpu();
    auto cpu_inv_scale = inv_scale.cpu();
    at::_amp_foreach_non_finite_check_and_unscale_(
        cpu_self, cpu_found_inf, cpu_inv_scale);
    for (size_t i = 0; i < self.size(); ++i) self[i].copy_(cpu_self[i]);
    found_inf.copy_(cpu_found_inf);
    return;
  }

  std::vector<at::Tensor> out;
  out.reserve(self.size());
  for (const auto& tensor : self) out.push_back(at::empty_like(tensor));
  gcu::TopsatenTensorList t_self(self);
  gcu::TopsatenTensorList t_out(out);
  gcu::TopsatenTensorWrapper t_found_inf(found_inf);
  gcu::TopsatenTensorWrapper t_inv_scale(inv_scale);
  EXEC_TOPSATEN_CMD(
      topsatenAmpForeachNonFiniteCheckAndUnscale, self[0], t_out.get(), t_found_inf.get(), t_self.get(),
      t_inv_scale.get());
  for (size_t i = 0; i < self.size(); ++i) self[i].copy_(out[i]);
}

REGISTER_IMPL_TO_DISPATCHER(PrivAmpForeachNonFiniteCheckAndUnscaleInplaceFn, priv_amp_foreach_non_finite_check_and_unscale_inplace_dispatcher, Backend::kGcu, PrivAmpForeachNonFiniteCheckAndUnscaleInplaceKernelGcu)

void PrivAmpForeachNonFiniteCheckAndUnscaleOutKernelGcu(at::TensorList self, at::Tensor& found_inf,
             const at::Tensor& inv_scale, at::TensorList out) {
  TORCH_CHECK(self.size() == out.size(),
              "priv_amp_foreach_non_finite_check_and_unscale_out_dispatcher: tensor lists must match in length");
  if (self.empty() || !gcu::IsForeachEligible(self) ||
      !gcu::IsForeachEligible(out) || !found_inf.defined() ||
      !inv_scale.defined() || !found_inf.is_contiguous() ||
      !inv_scale.is_contiguous() || found_inf.device() != self[0].device() ||
      inv_scale.device() != self[0].device() ||
      !gcu::TopsatenSupportsDtype(found_inf.scalar_type()) ||
      !gcu::TopsatenSupportsDtype(inv_scale.scalar_type())) {
    std::vector<at::Tensor> cpu_self;
    cpu_self.reserve(self.size());
    for (const auto& tensor : self) cpu_self.push_back(tensor.cpu());
    auto cpu_found_inf = found_inf.cpu();
    auto cpu_inv_scale = inv_scale.cpu();
    std::vector<at::Tensor> cpu_out;
    cpu_out.reserve(out.size());
    for (const auto& tensor : out) cpu_out.push_back(tensor.cpu());
    at::_amp_foreach_non_finite_check_and_unscale_outf(
        cpu_self, cpu_found_inf, cpu_inv_scale, cpu_out);
    for (size_t i = 0; i < out.size(); ++i) out[i].copy_(cpu_out[i]);
    // The reference leaves found_inf alone for this overload; propagate only
    // what it actually wrote so both routes agree.
    found_inf.copy_(cpu_found_inf);
    return;
  }

  // The CPU reference for the .out overload writes the unscaled values but
  // leaves found_inf untouched; topsaten updates it. Give the vendor call a
  // scratch flag so the observable contract matches the other backends.
  // GradScaler uses the in-place overload for overflow detection.
  auto scratch_found_inf = at::empty_like(found_inf);
  scratch_found_inf.copy_(found_inf);
  gcu::TopsatenTensorList t_self(self);
  gcu::TopsatenTensorList t_out(out);
  gcu::TopsatenTensorWrapper t_found_inf(scratch_found_inf);
  gcu::TopsatenTensorWrapper t_inv_scale(inv_scale);
  EXEC_TOPSATEN_CMD(
      topsatenAmpForeachNonFiniteCheckAndUnscale, self[0], t_out.get(), t_found_inf.get(), t_self.get(),
      t_inv_scale.get());
}

REGISTER_IMPL_TO_DISPATCHER(PrivAmpForeachNonFiniteCheckAndUnscaleOutFn, priv_amp_foreach_non_finite_check_and_unscale_out_dispatcher, Backend::kGcu, PrivAmpForeachNonFiniteCheckAndUnscaleOutKernelGcu)

at::Tensor ConvolutionOverrideableKernelGcu(
    const at::Tensor& input,
    const at::Tensor& weight,
    const ::std::optional<at::Tensor>& bias,
    at::IntArrayRef stride,
    at::IntArrayRef padding,
    at::IntArrayRef dilation,
    bool transposed,
    at::IntArrayRef output_padding,
    int64_t groups) {
  if (!gcu::TopsatenSupportsDtype(input.scalar_type()) ||
      !gcu::TopsatenSupportsDtype(weight.scalar_type()) || transposed) {
    auto r = at::convolution(
        input.cpu(), weight.cpu(),
        bias.has_value() && bias->defined()
            ? ::std::optional<at::Tensor>(bias->cpu())
            : ::std::nullopt,
        stride, padding, dilation, transposed, output_padding, groups);
    return r.to(input.device());
  }
  auto input_c = input.contiguous();
  auto weight_c = weight.contiguous();

  auto out_sizes = at::native::conv_output_size(
      input_c.sizes(), weight_c.sizes(), padding, stride, dilation);
  auto out = at::empty(out_sizes, input_c.options());

  // topsaten has no "absent bias" sentinel usable from here, so a zero bias
  // reproduces the unbiased result.
  at::Tensor bias_c = bias.has_value() && bias->defined()
      ? bias->contiguous()
      : at::zeros({weight_c.size(0)}, weight_c.options());

  gcu::TopsatenTensorWrapper t_out(out);
  gcu::TopsatenTensorWrapper t_input(input_c);
  gcu::TopsatenTensorWrapper t_weight(weight_c);
  gcu::TopsatenTensorWrapper t_bias(bias_c);
  gcu::TopsatenSizeWrapper w_stride(stride);
  gcu::TopsatenSizeWrapper w_padding(padding);
  gcu::TopsatenSizeWrapper w_dilation(dilation);
  gcu::TopsatenSizeWrapper w_output_padding(output_padding);
  EXEC_TOPSATEN_CMD(
      topsatenConvolution, input_c, t_out.get(), t_input.get(), t_weight.get(),
      t_bias.get(), w_stride.get(), w_padding.get(), w_dilation.get(),
      transposed, w_output_padding.get(), groups);
  return out;
}

REGISTER_IMPL_TO_DISPATCHER(ConvolutionOverrideableFn, convolution_overrideable_dispatcher, Backend::kGcu, ConvolutionOverrideableKernelGcu)

::std::tuple<at::Tensor, at::Tensor, at::Tensor> ConvolutionBackwardOverrideableKernelGcu(
    const at::Tensor& grad_output,
    const at::Tensor& input,
    const at::Tensor& weight,
    at::IntArrayRef stride,
    at::IntArrayRef padding,
    at::IntArrayRef dilation,
    bool transposed,
    at::IntArrayRef output_padding,
    int64_t groups,
    ::std::array<bool, 3> output_mask) {
  std::vector<int64_t> bias_sizes{weight.size(0)};
  auto r = at::convolution_backward(
      grad_output.cpu(), input.cpu(), weight.cpu(),
      ::std::optional<at::IntArrayRef>(at::IntArrayRef(bias_sizes)),
      stride, padding, dilation, transposed, output_padding, groups,
      output_mask);
  auto to_dev = [&](const at::Tensor& t) {
    return t.defined() ? t.to(input.device()) : t;
  };
  return {to_dev(std::get<0>(r)), to_dev(std::get<1>(r)),
          to_dev(std::get<2>(r))};
}

REGISTER_IMPL_TO_DISPATCHER(ConvolutionBackwardOverrideableFn, convolution_backward_overrideable_dispatcher, Backend::kGcu, ConvolutionBackwardOverrideableKernelGcu)

at::Tensor IndexSelectKernelGcu(const at::Tensor& self, int64_t dim, const at::Tensor& index) {
  // ATen raises IndexError for an out-of-range dim and for an index that is not
  // a vector or a scalar, and its messages are the useful ones, so both cases
  // are the host path's job. A 0-dim index is a legal one-element vector
  // (measured: shape (1, 4) for `index_select(t, 0, tensor(1))` on a (3, 4)),
  // which is why the flatten below is a reshape and not a rank check.
  const bool dim_ok =
      self.dim() != 0 && dim >= -self.dim() && dim < self.dim();
  if (!dim_ok || index.dim() > 1 ||
      !gcu::TopsatenSupportsDtype(self.scalar_type()) ||
      !gcu::TopsatenIndexDtype(index.scalar_type())) {
    return at::index_select(self.cpu(), dim, index.cpu()).to(self.device());
  }
  const int64_t dim_n = dim < 0 ? dim + self.dim() : dim;
  auto self_c = self.contiguous();
  auto index_c = index.to(self.device()).contiguous().reshape({-1});
  std::vector<int64_t> out_shape(self_c.sizes().vec());
  out_shape[dim_n] = index_c.size(0);
  auto out = at::empty(out_shape, self_c.options());
  if (out.numel() == 0) {
    return out;
  }
  if (!gcu::TopsatenIndexSelectFits({self_c, index_c, out})) {
    return at::index_select(self.cpu(), dim, index.cpu()).to(self.device());
  }

  gcu::TopsatenTensorWrapper t_self(self_c);
  gcu::TopsatenTensorWrapper t_index(index_c);
  gcu::TopsatenTensorWrapper t_out(out);
  EXEC_TOPSATEN_CMD(topsatenIndexSelect, out, t_out.get(), t_self.get(), dim_n, t_index.get());
  return out;
}

REGISTER_IMPL_TO_DISPATCHER(IndexSelectFn, index_select_dispatcher, Backend::kGcu, IndexSelectKernelGcu)

at::Tensor& IndexSelectOutKernelGcu(const at::Tensor& self, int64_t dim, const at::Tensor& index, at::Tensor& out) {
  gcu::TopsDeviceGuard out_guard(self);
  TORCH_CHECK(out.scalar_type() == self.scalar_type(),
              "index_select(): self and result must have the same scalar type");
  auto result = IndexSelectKernelGcu(self, dim, index);
  if (!out.sizes().equals(result.sizes())) {
    out.resize_(result.sizes());
  }
  if (out.numel() == 0) {
    return out;
  }
  out.copy_(result);
  return out;
}

REGISTER_IMPL_TO_DISPATCHER(IndexSelectOutFn, index_select_out_dispatcher, Backend::kGcu, IndexSelectOutKernelGcu)

at::Tensor& IndexFillInplaceIntScalarKernelGcu(at::Tensor& self, int64_t dim, const at::Tensor& index, const at::Scalar& value) {
  // Each way ATen rejects this call has a message worth keeping, and only the
  // host path raises it, so the checks come first: an out-of-range dim, an
  // index that is not a vector or a scalar, and an index that is not int64 --
  // the last applied even to an empty index (measured), which is why it cannot
  // be folded into the empty-index shortcut below.
  const bool dim_ok =
      self.dim() != 0 && dim >= -self.dim() && dim < self.dim();
  const bool index_ok = index.dim() <= 1 && index.scalar_type() == at::kLong;
  const bool self_ok =
      self.numel() != 0 && gcu::TopsatenSupportsDtype(self.scalar_type()) &&
      self.is_contiguous();
  const int64_t dim_n = dim_ok ? (dim < 0 ? dim + self.dim() : dim) : 0;
  at::Tensor index_c;
  if (dim_ok && index_ok && self_ok) {
    index_c = gcu::TopsatenIndexFillIndex(index, self.size(dim_n));
  }
  if (!index_c.defined()) {
    auto host = self.cpu();
    host.index_fill_(dim, index.cpu(), value);
    self.copy_(host);
    return self;
  }
  // Only reachable with dim and index both valid, so an empty index is the
  // no-op ATen makes it, and costs nothing here.
  if (index.numel() == 0) {
    return self;
  }
  auto t_value = gcu::ToTopsatenScalar(value, self.scalar_type());

  gcu::TopsatenTensorWrapper t_self(self);
  gcu::TopsatenTensorWrapper t_index(index_c);
  EXEC_TOPSATEN_CMD(topsatenIndexFill, self, t_self.get(), t_self.get(), dim_n, t_index.get(), t_value);
  return self;
}

REGISTER_IMPL_TO_DISPATCHER(IndexFillInplaceIntScalarFn, index_fill_inplace_int_scalar_dispatcher, Backend::kGcu, IndexFillInplaceIntScalarKernelGcu)

at::Tensor& IndexFillInplaceIntTensorKernelGcu(at::Tensor& self, int64_t dim, const at::Tensor& index, const at::Tensor& value) {
  const bool dim_ok =
      self.dim() != 0 && dim >= -self.dim() && dim < self.dim();
  const bool index_ok = index.dim() <= 1 && index.scalar_type() == at::kLong;
  const bool self_ok =
      self.numel() != 0 && gcu::TopsatenSupportsDtype(self.scalar_type()) &&
      self.is_contiguous();
  const int64_t dim_n = dim_ok ? (dim < 0 ? dim + self.dim() : dim) : 0;
  at::Tensor index_c;
  if (dim_ok && index_ok && self_ok && value.dim() == 0) {
    index_c = gcu::TopsatenIndexFillIndex(index, self.size(dim_n));
  }
  if (!index_c.defined()) {
    auto host = self.cpu();
    host.index_fill_(dim, index.cpu(), value.cpu());
    self.copy_(host);
    return self;
  }
  if (index.numel() == 0) {
    return self;
  }
  auto value_c = value.to(self.device(), self.scalar_type()).reshape({1});

  gcu::TopsatenTensorWrapper t_self(self);
  gcu::TopsatenTensorWrapper t_index(index_c);
  gcu::TopsatenTensorWrapper t_value(value_c);
  EXEC_TOPSATEN_CMD(topsatenIndexFill, self, t_self.get(), t_self.get(), dim_n, t_index.get(), t_value.get());
  return self;
}

REGISTER_IMPL_TO_DISPATCHER(IndexFillInplaceIntTensorFn, index_fill_inplace_int_tensor_dispatcher, Backend::kGcu, IndexFillInplaceIntTensorKernelGcu)

at::Tensor IndexFillIntScalarKernelGcu(const at::Tensor& self, int64_t dim, const at::Tensor& index, const at::Scalar& value) {
  auto result = self.clone();
  IndexFillInplaceIntScalarKernelGcu(result, dim, index, value);
  return result;
}

REGISTER_IMPL_TO_DISPATCHER(IndexFillIntScalarFn, index_fill_int_scalar_dispatcher, Backend::kGcu, IndexFillIntScalarKernelGcu)

at::Tensor& IndexFillIntScalarOutKernelGcu(const at::Tensor& self, int64_t dim, const at::Tensor& index, const at::Scalar& value, at::Tensor& out) {
  gcu::TopsDeviceGuard out_guard(self);
  TORCH_CHECK(out.scalar_type() == self.scalar_type(),
              "Expected out tensor to have dtype ", self.scalar_type(),
              ", but got ", out.scalar_type(), " instead");
  auto result = IndexFillIntScalarKernelGcu(self, dim, index, value);
  if (!out.sizes().equals(result.sizes())) {
    out.resize_(result.sizes());
  }
  if (out.numel() == 0) {
    return out;
  }
  out.copy_(result);
  return out;
}

REGISTER_IMPL_TO_DISPATCHER(IndexFillIntScalarOutFn, index_fill_int_scalar_out_dispatcher, Backend::kGcu, IndexFillIntScalarOutKernelGcu)

at::Tensor IndexFillIntTensorKernelGcu(const at::Tensor& self, int64_t dim, const at::Tensor& index, const at::Tensor& value) {
  auto result = self.clone();
  IndexFillInplaceIntTensorKernelGcu(result, dim, index, value);
  return result;
}

REGISTER_IMPL_TO_DISPATCHER(IndexFillIntTensorFn, index_fill_int_tensor_dispatcher, Backend::kGcu, IndexFillIntTensorKernelGcu)

at::Tensor& IndexFillIntTensorOutKernelGcu(const at::Tensor& self, int64_t dim, const at::Tensor& index, const at::Tensor& value, at::Tensor& out) {
  gcu::TopsDeviceGuard out_guard(self);
  TORCH_CHECK(out.scalar_type() == self.scalar_type(),
              "Expected out tensor to have dtype ", self.scalar_type(),
              ", but got ", out.scalar_type(), " instead");
  auto result = IndexFillIntTensorKernelGcu(self, dim, index, value);
  if (!out.sizes().equals(result.sizes())) {
    out.resize_(result.sizes());
  }
  if (out.numel() == 0) {
    return out;
  }
  out.copy_(result);
  return out;
}

REGISTER_IMPL_TO_DISPATCHER(IndexFillIntTensorOutFn, index_fill_int_tensor_out_dispatcher, Backend::kGcu, IndexFillIntTensorOutKernelGcu)

at::Tensor EmbeddingDenseBackwardKernelGcu(const at::Tensor& grad, const at::Tensor& indices, int64_t num_weights, int64_t padding_idx, bool scale_grad_by_freq) {
  // A padding_idx outside [-1, num_weights) has no vendor equivalent to lean
  // on. ATen compares it against the *flat* index, which is never negative, so
  // a value below -1 skips no row at all (measured: -4 and -1 both leave a
  // 6-row table untouched) and a value at or above num_weights matches no row
  // either, while topsaten's parameter names the row to skip. -1 is the "no
  // padding" sentinel both sides share -- it is topsaten's documented default.
  if (grad.dim() == 0 || num_weights <= 0 || padding_idx < -1 ||
      padding_idx >= num_weights ||
      !gcu::TopsatenEmbeddingDenseBackwardDtype(grad.scalar_type()) ||
      (indices.scalar_type() != at::kLong && indices.scalar_type() != at::kInt)) {
    return at::embedding_dense_backward(grad.cpu(), indices.cpu(), num_weights, padding_idx, scale_grad_by_freq)
        .to(grad.device());
  }
  auto grad_c = grad.contiguous();
  const int64_t embed_dim = grad_c.size(-1);
  auto indices_i32 = gcu::TopsatenEmbeddingIndex(indices, num_weights, grad.device());
  // topsaten rejects a mismatch with a status error, ATen raises its own
  // message for it, and ATen's is the one a caller can act on.
  const int64_t num_indices = indices_i32.defined() ? indices_i32.numel() : -1;
  if (!indices_i32.defined() || grad_c.numel() != num_indices * embed_dim) {
    return at::embedding_dense_backward(grad.cpu(), indices.cpu(), num_weights, padding_idx, scale_grad_by_freq)
        .to(grad.device());
  }
  auto out = at::empty({num_weights, embed_dim}, grad_c.options());
  if (out.numel() == 0) {
    return out;
  }
  if (num_indices == 0) {
    // topsatenEmbeddingDenseBackward zeroes its buffer as step 1 of its own
    // flow, so this is the one case that has to be zeroed here: no index means
    // no row is written, and ATen returns zeros.
    out.zero_();
    return out;
  }
  auto grad_2d = grad_c.reshape({-1, embed_dim});
  auto indices_2d = indices_i32.reshape({-1, 1});

  gcu::TopsatenTensorWrapper t_grad(grad_2d);
  gcu::TopsatenTensorWrapper t_indices(indices_2d);
  gcu::TopsatenTensorWrapper t_out(out);
  EXEC_TOPSATEN_CMD(topsatenEmbeddingDenseBackward, grad, t_out.get(), t_grad.get(), t_indices.get(), num_weights, padding_idx, scale_grad_by_freq);
  return out;
}

REGISTER_IMPL_TO_DISPATCHER(EmbeddingDenseBackwardFn, embedding_dense_backward_dispatcher, Backend::kGcu, EmbeddingDenseBackwardKernelGcu)

// The same guard _OUT_DEVICE_GUARD installs, spelled out because that snippet
// names `self` and this kernel's first parameter is `grad`. See its comment for
// why the resize below needs the device selected.
at::Tensor& EmbeddingDenseBackwardOutKernelGcu(const at::Tensor& grad, const at::Tensor& indices, int64_t num_weights, int64_t padding_idx, bool scale_grad_by_freq, at::Tensor& out) {
  gcu::TopsDeviceGuard out_guard(grad);
  TORCH_CHECK(out.scalar_type() == grad.scalar_type(),
              "Expected out tensor to have dtype ", grad.scalar_type(),
              ", but got ", out.scalar_type(), " instead");
  auto result = EmbeddingDenseBackwardKernelGcu(
      grad, indices, num_weights, padding_idx, scale_grad_by_freq);
  if (!out.sizes().equals(result.sizes())) {
    out.resize_(result.sizes());
  }
  if (out.numel() == 0) {
    return out;
  }
  out.copy_(result);
  return out;
}

REGISTER_IMPL_TO_DISPATCHER(EmbeddingDenseBackwardOutFn, embedding_dense_backward_out_dispatcher, Backend::kGcu, EmbeddingDenseBackwardOutKernelGcu)

at::Tensor PrivUpsampleNearestExact2dKernelGcu(const at::Tensor& self, at::IntArrayRef output_size, ::std::optional<double> scales_h, ::std::optional<double> scales_w) {
  if (self.dim() != 4 || output_size.size() != 2 || !scales_h.has_value() ||
      !scales_w.has_value() ||
      !gcu::TopsatenSupportsDtype(self.scalar_type())) {
    return at::_upsample_nearest_exact2d(self.cpu(), output_size, scales_h, scales_w).to(self.device());
  }
  auto self_c = self.contiguous();
  auto out = at::empty(
      {self_c.size(0), self_c.size(1), output_size[0], output_size[1]},
      self_c.options());
  if (out.numel() == 0) {
    return out;
  }
  auto t_scale_h = gcu::ToTopsatenScalar(at::Scalar(*scales_h), at::kDouble);
  auto t_scale_w = gcu::ToTopsatenScalar(at::Scalar(*scales_w), at::kDouble);
  gcu::TopsatenSizeWrapper t_size(output_size);

  gcu::TopsatenTensorWrapper t_self(self_c);
  gcu::TopsatenTensorWrapper t_out(out);
  EXEC_TOPSATEN_CMD(topsatenUpsampleNearestExact2d, self, t_out.get(), t_self.get(), t_size.get(), t_scale_h, t_scale_w);
  return out;
}

REGISTER_IMPL_TO_DISPATCHER(PrivUpsampleNearestExact2dFn, priv_upsample_nearest_exact2d_dispatcher, Backend::kGcu, PrivUpsampleNearestExact2dKernelGcu)

at::Tensor& PrivUpsampleNearestExact2dOutKernelGcu(const at::Tensor& self, at::IntArrayRef output_size, ::std::optional<double> scales_h, ::std::optional<double> scales_w, at::Tensor& out) {
  gcu::TopsDeviceGuard out_guard(self);
  TORCH_CHECK(out.scalar_type() == self.scalar_type(),
              "Expected out tensor to have dtype ", self.scalar_type(),
              ", but got ", out.scalar_type(), " instead");
  auto result =
      PrivUpsampleNearestExact2dKernelGcu(self, output_size, scales_h, scales_w);
  if (!out.sizes().equals(result.sizes())) {
    out.resize_(result.sizes());
  }
  if (out.numel() == 0) {
    return out;
  }
  out.copy_(result);
  return out;
}

REGISTER_IMPL_TO_DISPATCHER(PrivUpsampleNearestExact2dOutFn, priv_upsample_nearest_exact2d_out_dispatcher, Backend::kGcu, PrivUpsampleNearestExact2dOutKernelGcu)

void ForeachAddInplaceScalarKernelGcu(at::TensorList self, const at::Scalar& scalar) {
  if (!gcu::IsForeachEligible(self)) {
    for (const at::Tensor& t : self) t.add_(scalar);
    return;
  }
  auto t_scalar = gcu::ToTopsatenScalar(scalar, self[0].scalar_type());
  gcu::TopsatenTensorList t_self(self);
  gcu::TopsatenTensorList t_out(self);
  EXEC_TOPSATEN_CMD(topsatenForeachAdd, self[0], t_out.get(), t_self.get(), t_scalar);
}

REGISTER_IMPL_TO_DISPATCHER(ForeachAddInplaceScalarFn, foreach_add_inplace_scalar_dispatcher, Backend::kGcu, ForeachAddInplaceScalarKernelGcu)

void ForeachAddInplaceListKernelGcu(
    at::TensorList self,
    at::TensorList other,
    const at::Scalar& alpha) {
  if (!gcu::IsForeachEligible(self) || !gcu::IsForeachEligible(other) ||
      self.size() != other.size()) {
    for (size_t i = 0; i < self.size(); ++i) {
      self[i].add_(other[i], alpha);
    }
    return;
  }
  auto t_alpha = gcu::ToTopsatenScalar(alpha, self[0].scalar_type());
  gcu::TopsatenTensorList t_self(self);
  gcu::TopsatenTensorList t_other(other);
  gcu::TopsatenTensorList t_out(self);
  EXEC_TOPSATEN_CMD(
      topsatenForeachAdd, self[0], t_out.get(), t_self.get(), t_other.get(), t_alpha);
}

REGISTER_IMPL_TO_DISPATCHER(ForeachAddInplaceListFn, foreach_add_inplace_list_dispatcher, Backend::kGcu, ForeachAddInplaceListKernelGcu)

void ForeachAddInplaceTensorKernelGcu(
    at::TensorList self,
    const at::Tensor& other,
    const at::Scalar& alpha) {
  // aten requires `other` to be 0-dim here (it is typically a device-resident
  // learning rate). topsaten rejects rank-0 shapes, but the wrapper presents
  // those as shape {1} and the op broadcasts a 1-element rhs across every
  // list tensor -- verified on hardware.
  if (!gcu::IsForeachEligible(self) || !other.defined() ||
      other.numel() != 1 || !other.is_contiguous() ||
      !gcu::TopsatenSupportsDtype(other.scalar_type()) ||
      other.device() != self[0].device()) {
    for (const at::Tensor& t : self) t.add_(other, alpha);
    return;
  }
  auto t_alpha = gcu::ToTopsatenScalar(alpha, self[0].scalar_type());
  gcu::TopsatenTensorWrapper t_other(other);
  gcu::TopsatenTensorList t_self(self);
  gcu::TopsatenTensorList t_out(self);
  EXEC_TOPSATEN_CMD(
      topsatenForeachAdd, self[0], t_out.get(), t_self.get(), t_other.get(), t_alpha);
}

REGISTER_IMPL_TO_DISPATCHER(ForeachAddInplaceTensorFn, foreach_add_inplace_tensor_dispatcher, Backend::kGcu, ForeachAddInplaceTensorKernelGcu)

void ForeachSubInplaceScalarKernelGcu(at::TensorList self, const at::Scalar& scalar) {
  if (!gcu::IsForeachEligible(self)) {
    for (const at::Tensor& t : self) t.sub_(scalar);
    return;
  }
  auto t_scalar = gcu::ToTopsatenScalar(scalar, self[0].scalar_type());
  gcu::TopsatenTensorList t_self(self);
  gcu::TopsatenTensorList t_out(self);
  EXEC_TOPSATEN_CMD(topsatenForeachSub, self[0], t_out.get(), t_self.get(), t_scalar);
}

REGISTER_IMPL_TO_DISPATCHER(ForeachSubInplaceScalarFn, foreach_sub_inplace_scalar_dispatcher, Backend::kGcu, ForeachSubInplaceScalarKernelGcu)

void ForeachSubInplaceListKernelGcu(
    at::TensorList self,
    at::TensorList other,
    const at::Scalar& alpha) {
  if (!gcu::IsForeachEligible(self) || !gcu::IsForeachEligible(other) ||
      self.size() != other.size()) {
    for (size_t i = 0; i < self.size(); ++i) {
      self[i].sub_(other[i], alpha);
    }
    return;
  }
  auto t_alpha = gcu::ToTopsatenScalar(alpha, self[0].scalar_type());
  gcu::TopsatenTensorList t_self(self);
  gcu::TopsatenTensorList t_other(other);
  gcu::TopsatenTensorList t_out(self);
  EXEC_TOPSATEN_CMD(
      topsatenForeachSub, self[0], t_out.get(), t_self.get(), t_other.get(), t_alpha);
}

REGISTER_IMPL_TO_DISPATCHER(ForeachSubInplaceListFn, foreach_sub_inplace_list_dispatcher, Backend::kGcu, ForeachSubInplaceListKernelGcu)

void ForeachMulInplaceScalarKernelGcu(at::TensorList self, const at::Scalar& scalar) {
  if (!gcu::IsForeachEligible(self)) {
    for (const at::Tensor& t : self) t.mul_(scalar);
    return;
  }
  auto t_scalar = gcu::ToTopsatenScalar(scalar, self[0].scalar_type());
  gcu::TopsatenTensorList t_self(self);
  gcu::TopsatenTensorList t_out(self);
  EXEC_TOPSATEN_CMD(topsatenForeachMul, self[0], t_out.get(), t_self.get(), t_scalar);
}

REGISTER_IMPL_TO_DISPATCHER(ForeachMulInplaceScalarFn, foreach_mul_inplace_scalar_dispatcher, Backend::kGcu, ForeachMulInplaceScalarKernelGcu)

void ForeachMulInplaceListKernelGcu(at::TensorList self, at::TensorList other) {
  if (!gcu::IsForeachEligible(self) || !gcu::IsForeachEligible(other) ||
      self.size() != other.size()) {
    for (size_t i = 0; i < self.size(); ++i) self[i].mul_(other[i]);
    return;
  }
  gcu::TopsatenTensorList t_self(self);
  gcu::TopsatenTensorList t_other(other);
  gcu::TopsatenTensorList t_out(self);
  EXEC_TOPSATEN_CMD(
      topsatenForeachMul, self[0], t_out.get(), t_self.get(), t_other.get());
}

REGISTER_IMPL_TO_DISPATCHER(ForeachMulInplaceListFn, foreach_mul_inplace_list_dispatcher, Backend::kGcu, ForeachMulInplaceListKernelGcu)

void ForeachDivInplaceScalarKernelGcu(at::TensorList self, const at::Scalar& scalar) {
  if (!gcu::IsForeachEligible(self)) {
    for (const at::Tensor& t : self) t.div_(scalar);
    return;
  }
  auto t_scalar = gcu::ToTopsatenScalar(scalar, self[0].scalar_type());
  gcu::TopsatenTensorList t_self(self);
  gcu::TopsatenTensorList t_out(self);
  EXEC_TOPSATEN_CMD(topsatenForeachDiv, self[0], t_out.get(), t_self.get(), t_scalar);
}

REGISTER_IMPL_TO_DISPATCHER(ForeachDivInplaceScalarFn, foreach_div_inplace_scalar_dispatcher, Backend::kGcu, ForeachDivInplaceScalarKernelGcu)

void ForeachDivInplaceListKernelGcu(at::TensorList self, at::TensorList other) {
  if (!gcu::IsForeachEligible(self) || !gcu::IsForeachEligible(other) ||
      self.size() != other.size()) {
    for (size_t i = 0; i < self.size(); ++i) self[i].div_(other[i]);
    return;
  }
  gcu::TopsatenTensorList t_self(self);
  gcu::TopsatenTensorList t_other(other);
  gcu::TopsatenTensorList t_out(self);
  EXEC_TOPSATEN_CMD(
      topsatenForeachDiv, self[0], t_out.get(), t_self.get(), t_other.get());
}

REGISTER_IMPL_TO_DISPATCHER(ForeachDivInplaceListFn, foreach_div_inplace_list_dispatcher, Backend::kGcu, ForeachDivInplaceListKernelGcu)

void ForeachDivInplaceScalarlistKernelGcu(at::TensorList self, at::ArrayRef<at::Scalar> scalars) {
  if (!gcu::IsForeachEligible(self) || scalars.size() != self.size()) {
    for (size_t i = 0; i < self.size(); ++i) self[i].div_(scalars[i]);
    return;
  }
  std::vector<topsatenScalar_t> t_scalars;
  t_scalars.reserve(scalars.size());
  for (size_t i = 0; i < scalars.size(); ++i) {
    t_scalars.push_back(
        gcu::ToTopsatenScalar(scalars[i], self[i].scalar_type()));
  }
  gcu::TopsatenTensorList t_self(self);
  gcu::TopsatenTensorList t_out(self);
  EXEC_TOPSATEN_CMD(topsatenForeachDiv, self[0], t_out.get(), t_self.get(), t_scalars);
}

REGISTER_IMPL_TO_DISPATCHER(ForeachDivInplaceScalarlistFn, foreach_div_inplace_scalarlist_dispatcher, Backend::kGcu, ForeachDivInplaceScalarlistKernelGcu)

void ForeachNegInplaceKernelGcu(at::TensorList self) {
  if (!gcu::IsForeachEligible(self)) {
    for (const at::Tensor& t : self) t.neg_();
    return;
  }
  gcu::TopsatenTensorList t_self(self);
  gcu::TopsatenTensorList t_out(self);
  EXEC_TOPSATEN_CMD(topsatenForeachNeg, self[0], t_out.get(), t_self.get());
}

REGISTER_IMPL_TO_DISPATCHER(ForeachNegInplaceFn, foreach_neg_inplace_dispatcher, Backend::kGcu, ForeachNegInplaceKernelGcu)

void ForeachSqrtInplaceKernelGcu(at::TensorList self) {
  if (!gcu::IsForeachEligible(self)) {
    for (const at::Tensor& t : self) t.sqrt_();
    return;
  }
  gcu::TopsatenTensorList t_self(self);
  gcu::TopsatenTensorList t_out(self);
  EXEC_TOPSATEN_CMD(topsatenForeachSqrt, self[0], t_out.get(), t_self.get());
}

REGISTER_IMPL_TO_DISPATCHER(ForeachSqrtInplaceFn, foreach_sqrt_inplace_dispatcher, Backend::kGcu, ForeachSqrtInplaceKernelGcu)

::std::vector<at::Tensor> ForeachSqrtKernelGcu(at::TensorList self) {
  if (!gcu::IsForeachEligible(self)) {
    std::vector<at::Tensor> out;
    out.reserve(self.size());
    for (const at::Tensor& t : self) out.push_back(t.sqrt());
    return out;
  }
  std::vector<at::Tensor> out;
  out.reserve(self.size());
  for (const at::Tensor& t : self) {
    out.push_back(at::empty(t.sizes(), t.options()));
  }
  gcu::TopsatenTensorList t_self(self);
  gcu::TopsatenTensorList t_out(out);
  EXEC_TOPSATEN_CMD(topsatenForeachSqrt, self[0], t_out.get(), t_self.get());
  return out;
}

REGISTER_IMPL_TO_DISPATCHER(ForeachSqrtFn, foreach_sqrt_dispatcher, Backend::kGcu, ForeachSqrtKernelGcu)

void ForeachLerpInplaceScalarKernelGcu(
    at::TensorList self,
    at::TensorList tensors1,
    const at::Scalar& weight) {
  if (!gcu::IsForeachEligible(self) || !gcu::IsForeachEligible(tensors1) ||
      self.size() != tensors1.size()) {
    for (size_t i = 0; i < self.size(); ++i) {
      self[i].lerp_(tensors1[i], weight);
    }
    return;
  }
  auto t_weight = gcu::ToTopsatenScalar(weight, self[0].scalar_type());
  gcu::TopsatenTensorList t_self(self);
  gcu::TopsatenTensorList t_end(tensors1);
  gcu::TopsatenTensorList t_out(self);
  EXEC_TOPSATEN_CMD(
      topsatenForeachLerp, self[0], t_out.get(), t_self.get(), t_end.get(), t_weight);
}

REGISTER_IMPL_TO_DISPATCHER(ForeachLerpInplaceScalarFn, foreach_lerp_inplace_scalar_dispatcher, Backend::kGcu, ForeachLerpInplaceScalarKernelGcu)

void ForeachAddcmulInplaceScalarKernelGcu(
    at::TensorList self,
    at::TensorList tensor1,
    at::TensorList tensor2,
    const at::Scalar& value) {
  if (!gcu::IsForeachEligible(self) || !gcu::IsForeachEligible(tensor1) ||
      !gcu::IsForeachEligible(tensor2) || self.size() != tensor1.size() ||
      self.size() != tensor2.size()) {
    for (size_t i = 0; i < self.size(); ++i) {
      self[i].addcmul_(tensor1[i], tensor2[i], value);
    }
    return;
  }
  auto t_value = gcu::ToTopsatenScalar(value, self[0].scalar_type());
  gcu::TopsatenTensorList t_self(self);
  gcu::TopsatenTensorList t_t1(tensor1);
  gcu::TopsatenTensorList t_t2(tensor2);
  gcu::TopsatenTensorList t_out(self);
  EXEC_TOPSATEN_CMD(
      topsatenForeachAddcmul, self[0], t_out.get(), t_self.get(), t_t1.get(), t_t2.get(),
      t_value);
}

REGISTER_IMPL_TO_DISPATCHER(ForeachAddcmulInplaceScalarFn, foreach_addcmul_inplace_scalar_dispatcher, Backend::kGcu, ForeachAddcmulInplaceScalarKernelGcu)

void ForeachAddcmulInplaceScalarlistKernelGcu(
    at::TensorList self,
    at::TensorList tensor1,
    at::TensorList tensor2,
    at::ArrayRef<at::Scalar> scalars) {
  if (!gcu::IsForeachEligible(self) || !gcu::IsForeachEligible(tensor1) ||
      !gcu::IsForeachEligible(tensor2) || self.size() != tensor1.size() ||
      self.size() != tensor2.size() || scalars.size() != self.size()) {
    for (size_t i = 0; i < self.size(); ++i) {
      self[i].addcmul_(tensor1[i], tensor2[i], scalars[i]);
    }
    return;
  }
  std::vector<topsatenScalar_t> t_scalars;
  t_scalars.reserve(scalars.size());
  for (size_t i = 0; i < scalars.size(); ++i) {
    t_scalars.push_back(
        gcu::ToTopsatenScalar(scalars[i], self[i].scalar_type()));
  }
  gcu::TopsatenTensorList t_self(self);
  gcu::TopsatenTensorList t_t1(tensor1);
  gcu::TopsatenTensorList t_t2(tensor2);
  gcu::TopsatenTensorList t_out(self);
  EXEC_TOPSATEN_CMD(
      topsatenForeachAddcmul, self[0], t_out.get(), t_self.get(), t_t1.get(), t_t2.get(),
      t_scalars);
}

REGISTER_IMPL_TO_DISPATCHER(ForeachAddcmulInplaceScalarlistFn, foreach_addcmul_inplace_scalarlist_dispatcher, Backend::kGcu, ForeachAddcmulInplaceScalarlistKernelGcu)

void ForeachAddcdivInplaceScalarKernelGcu(
    at::TensorList self,
    at::TensorList tensor1,
    at::TensorList tensor2,
    const at::Scalar& value) {
  if (!gcu::IsForeachEligible(self) || !gcu::IsForeachEligible(tensor1) ||
      !gcu::IsForeachEligible(tensor2) || self.size() != tensor1.size() ||
      self.size() != tensor2.size()) {
    for (size_t i = 0; i < self.size(); ++i) {
      self[i].addcdiv_(tensor1[i], tensor2[i], value);
    }
    return;
  }
  auto t_value = gcu::ToTopsatenScalar(value, self[0].scalar_type());
  gcu::TopsatenTensorList t_self(self);
  gcu::TopsatenTensorList t_t1(tensor1);
  gcu::TopsatenTensorList t_t2(tensor2);
  gcu::TopsatenTensorList t_out(self);
  EXEC_TOPSATEN_CMD(
      topsatenForeachAddcdiv, self[0], t_out.get(), t_self.get(), t_t1.get(), t_t2.get(),
      t_value);
}

REGISTER_IMPL_TO_DISPATCHER(ForeachAddcdivInplaceScalarFn, foreach_addcdiv_inplace_scalar_dispatcher, Backend::kGcu, ForeachAddcdivInplaceScalarKernelGcu)

void ForeachAddcdivInplaceScalarlistKernelGcu(
    at::TensorList self,
    at::TensorList tensor1,
    at::TensorList tensor2,
    at::ArrayRef<at::Scalar> scalars) {
  if (!gcu::IsForeachEligible(self) || !gcu::IsForeachEligible(tensor1) ||
      !gcu::IsForeachEligible(tensor2) || self.size() != tensor1.size() ||
      self.size() != tensor2.size() || scalars.size() != self.size()) {
    for (size_t i = 0; i < self.size(); ++i) {
      self[i].addcdiv_(tensor1[i], tensor2[i], scalars[i]);
    }
    return;
  }
  std::vector<topsatenScalar_t> t_scalars;
  t_scalars.reserve(scalars.size());
  for (size_t i = 0; i < scalars.size(); ++i) {
    t_scalars.push_back(
        gcu::ToTopsatenScalar(scalars[i], self[i].scalar_type()));
  }
  gcu::TopsatenTensorList t_self(self);
  gcu::TopsatenTensorList t_t1(tensor1);
  gcu::TopsatenTensorList t_t2(tensor2);
  gcu::TopsatenTensorList t_out(self);
  EXEC_TOPSATEN_CMD(
      topsatenForeachAddcdiv, self[0], t_out.get(), t_self.get(), t_t1.get(), t_t2.get(),
      t_scalars);
}

REGISTER_IMPL_TO_DISPATCHER(ForeachAddcdivInplaceScalarlistFn, foreach_addcdiv_inplace_scalarlist_dispatcher, Backend::kGcu, ForeachAddcdivInplaceScalarlistKernelGcu)

at::Tensor WhereSelfKernelGcu(
    const at::Tensor& condition, const at::Tensor& self, const at::Tensor& other) {
  auto result_dtype = at::result_type(self, other);
  if (condition.scalar_type() != at::kBool ||
      !gcu::TopsatenWhereDtype(result_dtype)) {
    return at::where(condition.cpu(), self.cpu(), other.cpu()).to(self.device());
  }
  auto cond_c = condition.to(self.device(), at::kBool);
  auto self_c = self.scalar_type() == result_dtype ? self : self.to(result_dtype);
  auto other_c = other.to(self.device(), result_dtype);
  // All three operands are expanded, not just the two values: the vendor does not
  // broadcast, and ATen broadcasts the condition against the values' common
  // shape, which is not the same thing when the condition is the wider one.
  auto out_shape = at::infer_size(
      at::infer_size(cond_c.sizes(), self_c.sizes()), other_c.sizes());
  auto cond_b = cond_c.expand(out_shape).contiguous();
  auto self_b = self_c.expand(out_shape).contiguous();
  auto other_b = other_c.expand(out_shape).contiguous();
  auto out = at::empty(out_shape, self_b.options());
  if (out.numel() == 0) {
    return out;
  }

  gcu::TopsatenTensorWrapper t_cond(cond_b);
  gcu::TopsatenTensorWrapper t_self(self_b);
  gcu::TopsatenTensorWrapper t_other(other_b);
  gcu::TopsatenTensorWrapper t_out(out);
  EXEC_TOPSATEN_CMD(
      topsatenWhere, out, t_out.get(), t_cond.get(), t_self.get(), t_other.get());
  return out;
}

REGISTER_IMPL_TO_DISPATCHER(WhereSelfFn, where_self_dispatcher, Backend::kGcu, WhereSelfKernelGcu)

at::Tensor AllKernelGcu(const at::Tensor& self) {
  auto operand = gcu::TopsatenAllOperand(self);
  if (!operand.defined() || self.numel() == 0) {
    return at::all(self.cpu()).to(self.device());
  }
  // ATen's dtype inference is not uniformly bool here: `all` on a uint8
  // operand is uint8, on every other dtype bool. That is the *meta* function,
  // not a CPU kernel quirk, so a backend that answers bool is the deviant one.
  // The vendor writes the correct 0/1 into either descriptor (measured on card
  // 4), so the declared dtype is the one to allocate.
  auto out = at::empty(
      {}, self.options().dtype(
                self.scalar_type() == at::kByte ? at::kByte : at::kBool));

  gcu::TopsatenTensorWrapper t_self(operand);
  gcu::TopsatenTensorWrapper t_out(out);
  EXEC_TOPSATEN_CMD(topsatenAll, out, t_out.get(), t_self.get());
  return out;
}

REGISTER_IMPL_TO_DISPATCHER(AllFn, all_dispatcher, Backend::kGcu, AllKernelGcu)

at::Tensor IndexTensorKernelGcu(
    const at::Tensor& self,
    const c10::List<::std::optional<at::Tensor>>& indices) {
  if (indices.size() == 1 && indices[0].has_value() &&
      indices[0]->defined()) {
    const at::Tensor& index = indices[0].value();
    if (index.dim() == 1 && gcu::TopsatenIndexDtype(index.scalar_type()) &&
        gcu::TopsatenIndexNonNegative(index)) {
      // The delegate carries its own guard: a 0-dim self, an unsupported
      // operand dtype or a non-contiguous operand all fall out of it onto the
      // same host path this template uses below, so there is nothing to test
      // here that index_select does not already test.
      return IndexSelectKernelGcu(self, 0, index);
    }
  }
  // The indices travel with the operand: ATen's CPU advanced indexing runs on
  // whichever device holds the data, and a device index would otherwise reach it
  // as a device pointer. This is the same marshalling register.cc's
  // WrapperIndexPut_ does.
  c10::List<::std::optional<at::Tensor>> indices_cpu;
  for (int64_t i = 0; i < static_cast<int64_t>(indices.size()); ++i) {
    auto opt = indices.get(i);
    if (opt.has_value() && opt->defined()) {
      indices_cpu.push_back(opt->cpu());
    } else {
      indices_cpu.push_back(::std::nullopt);
    }
  }
  return at::index(self.cpu(), indices_cpu).to(self.device());
}

REGISTER_IMPL_TO_DISPATCHER(IndexTensorFn, index_tensor_dispatcher, Backend::kGcu, IndexTensorKernelGcu)

at::Tensor NonzeroKernelGcu(const at::Tensor& self) {
  auto operand = gcu::TopsatenNonzeroOperand(self);
  // A rank-0 operand is not a coverage gap: ATen's result for one is (1, 0), a
  // nonzero number of rows with zero columns, which no topsaten output can
  // describe. An empty operand is declined for the same kind of reason -- there
  // is nothing to read, and a zero-element descriptor is not worth handing to
  // the driver.
  if (!operand.defined() || self.dim() == 0 || self.numel() == 0) {
    return at::nonzero(self.cpu()).to(self.device());
  }
  const int64_t rank = self.dim();
  auto count = at::empty({1}, self.options().dtype(at::kInt));
  gcu::TopsatenTensorWrapper t_operand(operand);
  EXEC_TOPSATEN_CMD(
      topsatenCountNonzero, self, reinterpret_cast<int32_t*>(count.data_ptr()),
      t_operand.get());
  const auto count_host = count.cpu();
  const int64_t nnz = count_host.const_data_ptr<int32_t>()[0];

  auto out = at::empty({nnz, rank}, self.options().dtype(at::kLong));
  if (nnz == 0) {
    return out;
  }
  auto staging = at::empty({nnz, rank}, self.options().dtype(at::kInt));
  gcu::TopsatenTensorWrapper t_staging(staging);
  EXEC_TOPSATEN_CMD(topsatenNonzero, self, t_staging.get(), t_operand.get());

  topsatenDataType_t want = TOPSATEN_DATA_I64;
  gcu::TopsatenTensorWrapper t_dst(out);
  EXEC_TOPSATEN_CMD(
      topsatenTo, self, t_dst.get(), t_staging.get(), want, false, false,
      TOPSATEN_MEMORY_PRESERVE);
  return out;
}

REGISTER_IMPL_TO_DISPATCHER(NonzeroFn, nonzero_dispatcher, Backend::kGcu, NonzeroKernelGcu)

at::Tensor NonzeroStaticKernelGcu(
    const at::Tensor& self, int64_t size, int64_t fill_value) {
  if (size < 0) {
    // ATen's own message -- "nonzero_static: 'size' must be an non-negative
    // integer" -- and its own kernel is the only spelling of it that cannot
    // drift from ATen.
    return at::nonzero_static(self.cpu(), size, fill_value).to(self.device());
  }
  auto operand = gcu::TopsatenNonzeroOperand(self);
  if (!operand.defined() || self.dim() == 0 || self.numel() == 0) {
    return at::nonzero_static(self.cpu(), size, fill_value).to(self.device());
  }
  const int64_t rank = self.dim();
  if (size == 0) {
    return at::empty({size, rank}, self.options().dtype(at::kLong));
  }
  auto count = at::empty({1}, self.options().dtype(at::kInt));
  gcu::TopsatenTensorWrapper t_operand(operand);
  EXEC_TOPSATEN_CMD(
      topsatenCountNonzero, self, reinterpret_cast<int32_t*>(count.data_ptr()),
      t_operand.get());
  const auto count_host = count.cpu();
  const int64_t nnz = count_host.const_data_ptr<int32_t>()[0];

  // Nothing to fill when the coordinates already reach `size`, so the staging is
  // exactly as wide as the call needs.
  const bool fill = size > nnz;
  if (fill && (fill_value < std::numeric_limits<int32_t>::min() ||
               fill_value > std::numeric_limits<int32_t>::max())) {
    // The padding lands in an int32 staging buffer, so a fill_value that does
    // not survive the round trip would be truncated on the way in and widened
    // back wrong. ATen accepts any int64, so that case takes the host path
    // rather than producing a narrowed tail.
    return at::nonzero_static(self.cpu(), size, fill_value).to(self.device());
  }
  const int64_t rows = fill ? size : nnz;
  auto staging = at::empty({rows, rank}, self.options().dtype(at::kInt));
  if (fill) {
    gcu::TopsatenTensorWrapper t_staging(staging);
    topsatenScalar_t value = gcu::ToTopsatenScalar(at::Scalar(fill_value), at::kInt);
    EXEC_TOPSATEN_CMD(topsatenFill_, staging, t_staging.get(), value);
  }
  if (nnz > 0) {
    gcu::TopsatenRowView stage_rows(staging, nnz, rank);
    EXEC_TOPSATEN_CMD(topsatenNonzero, self, stage_rows.get(), t_operand.get());
  }

  auto out = at::empty({size, rank}, self.options().dtype(at::kLong));
  topsatenDataType_t want = TOPSATEN_DATA_I64;
  gcu::TopsatenTensorWrapper t_dst(out);
  // Equal to the staging when size >= nnz and a prefix of it when size < nnz;
  // both are one copy of the first `size` rows.
  gcu::TopsatenRowView stage_out(staging, size, rank);
  EXEC_TOPSATEN_CMD(
      topsatenTo, self, t_dst.get(), stage_out.get(), want, false, false,
      TOPSATEN_MEMORY_PRESERVE);
  return out;
}

REGISTER_IMPL_TO_DISPATCHER(NonzeroStaticFn, nonzero_static_dispatcher, Backend::kGcu, NonzeroStaticKernelGcu)

} // namespace at::native::flagos
